# ツカイドキ PJ詳細画面 改修指示書

最終更新：2026年5月

---

## 概要

ツカイドキの**PJ詳細画面**を改修する。
現状の画面（試算ベース）から、**予実管理（計画 vs 実績）モデル**へ変更する。

このドキュメントはPJ詳細画面に関わる差分指示書。
他の画面（ホーム、使いみち、プール金、シナリオ等）の仕様は別途指示書を参照。

---

## 目次

1. [改修の方針](#1-改修の方針)
2. [画面全体の構成](#2-画面全体の構成)
3. [折れ線グラフ仕様](#3-折れ線グラフ仕様)
4. [積立/支出タブ](#4-積立支出タブ)
5. [計画/実績の年表（2カラム）](#5-計画実績の年表2カラム)
6. [カード選択 → ハイライト方式](#6-カード選択--ハイライト方式)
7. [下部の編集ボタン](#7-下部の編集ボタン)
8. [4種類の編集ポップアップ](#8-4種類の編集ポップアップ)
9. [リアルタイムグラフ反映](#9-リアルタイムグラフ反映)
10. [表記ルール](#10-表記ルール)
11. [カラー仕様](#11-カラー仕様)
12. [実装チェックリスト](#12-実装チェックリスト)

---

## 1. 改修の方針

### 旧方針（廃止）

「すべては試算」という思想で、計画と実績を区別せず、すべてを未来の試算として扱っていた。

### 新方針（採用）

**予実管理（計画 vs 実績）モデル**に戻す。
- ユーザーは「自分はちゃんと貯められているか」を確認したい
- 計画と実績を比較して、修正が必要な箇所を可視化する
- 過去の振り返りと、未来の試算を両立する

### デザインの基本方針

- 緑系のメインカラー（#1D9E75）を維持
- AIインサイトのスタイル維持
- フォント・余白・角丸などの統一感は維持
- **折れ線グラフ**は、計画(青破線)・実績(緑実線)・支出(赤丸)の3要素を持つ

---

## 2. 画面全体の構成

画面は上から順に以下の構造で配置する。

```
┌──────────────────────────┐
│ ヘッダー                   │
│ ‹ 使いみち（戻る）          │
│ 教育資金       現在の積み立て│
│ 2044年大学入学  430万円     │
│              目標500万円    │
├──────────────────────────┤
│ 折れ線グラフカード          │
│ 凡例 / 期間タブ            │
│ [グラフ本体]               │
├──────────────────────────┤
│ [📈 積立] [💸 支出]        │ ← 2タブ
├──────────────────────────┤
│ [計画]      [実績]         │ ← 2カラムヘッダー
├──────────────────────────┤
│ 年表エリア（スクロール）     │
│ ┌──────────────────────┐ │
│ │ 計画カード │ 実績カード │ │
│ └──────────────────────┘ │
│ ┌──────────────────────┐ │
│ │ 計画カード │ + 記録    │ │
│ └──────────────────────┘ │
├──────────────────────────┤
│ [カードを選んで編集] ← 下部固定│
└──────────────────────────┘
```

### サイズの目安（縦比率）

- ヘッダー：12%
- グラフカード：28%
- 積立/支出タブ＋計画/実績ヘッダー：8%
- 年表エリア：残りスクロール
- 下部固定ボタン：env-safe-area込み

---

## 3. 折れ線グラフ仕様

### 表示要素

| 要素 | スタイル |
|---|---|
| 計画ライン | **青の破線**（#185FA5、stroke-dasharray: 4 3、opacity 0.7、stroke-width 1.5） |
| 実績ライン | **緑の実線**（#1D9E75、stroke-width 2、stroke-linecap round） |
| 支出マーカー | **赤丸**（#E24B4A、r=5）+ 縦の点線（#E24B4A、opacity 0.3） |
| マイルストーン | 円（色は時期によって青/オレンジ/緑、r=4、白枠） |

### 凡例

グラフ上部に凡例を表示：
```
─ ─ 計画   ── 実績   ● 支出   単位:万円
```

### 期間切替タブ

グラフ右上に「生涯」「5年」「1年」のタブを配置。
- アクティブ：青背景（#185FA5）+ 白文字
- 非アクティブ：透明背景 + グレー枠

### Y軸ラベル

**数字のみ**（「100」「200」「300」「400」）。
凡例横に「単位:万円」と明示する。

### X軸ラベル

10年刻みで短縮表記（"'25" "'35" "'45"）。

### ハイライト機能（重要）

カード選択時、**該当年の点が脈動光る**：
- ハイライト位置に半透明の円を2層重ねる（r=12 opacity 0.15、r=7 opacity 0.3）
- JavaScriptの`setInterval`で透明度を周期的に変化させる（周期1秒程度）
- カード選択解除時、ハイライトを消す

---

## 4. 積立/支出タブ

折れ線グラフの直下に、横並びの2タブを配置。

```
[ 📈 積立 ]  [ 💸 支出 ]
```

### スタイル

- アクティブ：**緑背景**（#1D9E75）+ 白文字 + アイコン
- 非アクティブ：透明背景 + グレー文字 + 細い枠
- アイコン：絵文字でOK（📈、💸）

### 切替時の挙動

- 積立タブ → 積立の年表を表示
- 支出タブ → 支出の年表を表示
- **タブ切替時、選択中のカードはクリア**（編集ボタンも無効化に戻る）

---

## 5. 計画/実績の年表（2カラム）

### 構造

積立/支出タブの直下に、2カラムのヘッダー：

```
┌──────────┬──────────┐
│   計画    │   実績    │ ← カラムヘッダー
└──────────┴──────────┘
   青下線      緑下線
```

その下に、横並びの**年表カード**を縦にリスト表示。

### 1カードの構造

```
┌────────────────┬─┬────────────────┐
│ 2030年〜        │●│ 2030年          │
│ 月5万円に増額   │ │ + 記録する      │
│ ピークに備える  │ │                │
│ +5万円/月       │ │                │
└────────────────┴─┴────────────────┘
   計画側（左）    中央  実績側（右）
```

### カードの中央ドット

- 計画も実績も入力済み：**緑塗りつぶし**
- 計画のみ入力済み（実績未入力）：**白抜き丸**
- 支出マイルストーン：**赤塗りつぶし**

### 年表に表示する内容

#### 「積立」タブの年表

時系列で**積立額の変化点**だけをリスト表示：
- 2025年〜：月3万円 積立スタート
- 2030年〜：月5万円に増額
- 2044年〜：積立停止

各カードには「**+◯万円/月**」の表記。

#### 「支出」タブの年表

時系列で**支出イベント**をリスト表示（イベント名と年額）：
- 2027年〜：保育園費用 -72万円/年
- 2033年〜：小学校 -31万円/年
- 2044年：大学入学 -100万円

### 「未入力」カードの表示

実績側で未入力の場合：
```
┌────────────┐
│ + 記録する  │ ← +アイコン + テキスト
│ 破線枠     │
└────────────┘
```

- 破線枠（dashed border）
- 中央配置でアイコン + テキスト
- グレー系の色

---

## 6. カード選択 → ハイライト方式

**重要：カードをタップしても編集画面はすぐに開かない。**

### 操作フロー

```
1. カードをタップ
   ↓
2. カードが選択状態になる（青背景+青枠）
   ↓
3. 折れ線グラフの該当年がほわほわ光り始める
   ↓
4. 下部のボタンが「○○を編集」に変わり、活性化
   ↓
5. ボタンを押す
   ↓
6. 編集ポップアップが開く
```

### 選択状態のスタイル

カードに `.selected` クラスを付与：
- 背景色：薄い青（#E6F1FB）
- 枠線：青（#185FA5、1.5px）
- transition: 0.2s で滑らかに変化

### 同じカード再タップ時

- 選択を解除
- ハイライト消す
- ボタン無効化

### タブ切替時

- 選択をクリア
- ボタン無効化

### グラフのハイライト位置の判定

カードに `data-year-idx` 属性を付与しておき、それを使ってグラフ上の位置を特定：
```html
<div class="event-side" data-card-id="ps-2" data-type="plan-saving" data-year-idx="2">
```

実績側の場合は `data-year` 属性（年だけ）から逆引き：
```html
<div class="event-side" data-card-id="as-2" data-type="actual-saving" data-year="2030">
```

---

## 7. 下部の編集ボタン

下部固定ボタンとして配置（旧「積立を調整する」を置き換え）。

### 状態別表示

| 状態 | テキスト | スタイル |
|---|---|---|
| 未選択 | カードを選んで編集 | 無効化（グレー） |
| 積立計画選択 | 積立計画を編集 | 緑、活性化 |
| 積立実績選択 | 積立実績を記録 | 緑、活性化 |
| 支出計画選択 | 支出計画を編集 | 緑、活性化 |
| 支出実績選択 | 支出実績を記録 | 緑、活性化 |

### スタイル

- 横幅：コンテナいっぱい（左右16px余白）
- パディング：13px
- 角丸：14px
- フォント：14px、weight 600
- 背景：#1D9E75（活性時）／ rgba(0,0,0,0.15)（無効時）
- 上方向にグラデーション背景でフェード（背景に溶け込ませる）

---

## 8. 4種類の編集ポップアップ

下からスライドインするポップアップ。
**画面下部60%（実績入力は80%）**に表示し、上部のグラフは見える状態を維持する。

### 共通構造

```
┌────────────────────┐
│        ─           │ ← ハンドル
│ タイトル           │
│ サブテキスト       │
│                    │
│ ボディ部分         │
│ （スクロール可能）  │
│                    │
│ [閉じる] [保存する] │
└────────────────────┘
```

- ハンドル：上部中央の小さな水平バー（タップ可能性を示唆）
- タイトル：17px、weight 600
- サブ：11px、グレー
- ボディ：縦スクロール可能
- アクション：横並び2ボタン

### 8-1. 積立計画ポップアップ

**タイトル：いくら積み立てる？**

```
┌────────────────────┐
│ いくら積み立てる？   │
│ 計画 ・ 2030年〜     │
│                    │
│ 適用開始            │
│ [ 2030 ] 年〜       │
│                    │
│ 月の積立額          │
│ [−]   5 万円/月  [+] │
│                    │
│ [閉じる] [保存する] │
└────────────────────┘
```

#### 入力項目

1. **適用開始**：年の数値入力 + 「年〜」ラベル
2. **月の積立額**：±ボタンで増減、中央に大きく表示

#### スタイル

- 年入力：通常の数値input
- 積立額：左右に36pxの丸ボタン、中央に24px大きさで「5万円/月」表示
- ±ボタンを押すたびに**金額が変化し、上部のグラフがリアルタイム更新**

### 8-2. 支出計画ポップアップ

**タイトル：いつ、いくら使う予定？**

```
┌────────────────────┐
│ いつ、いくら使う予定？│
│ 計画 ・ 保育園費用   │
│                    │
│ 時期                │
│ [ 2027 ] 年〜       │
│                    │
│ 単位                │
│ [年単位] [月単位]   │
│                    │
│ 金額                │
│ [−]   72 万円/年 [+]│
│                    │
│ [閉じる] [保存する] │
└────────────────────┘
```

#### 入力項目

1. **時期**：年の数値入力
2. **単位切替**：「年単位」「月単位」のタブ
3. **金額**：±ボタンで増減

#### 単位切替の挙動

- 「年単位」アクティブ：金額の単位ラベル「万円/年」
- 「月単位」アクティブ：金額の単位ラベル「万円/月」
- アクティブタブは青背景（#185FA5）+ 白文字

#### リアルタイム反映

- ±で金額を変えると、グラフの該当支出マーカーの位置が変動

### 8-3. 積立実績ポップアップ（大型・80vh）

**タイトル：積立を記録する**

```
┌────────────────────┐
│ 積立を記録する       │
│ 実績 ・ 2025年       │
│                    │
│ 月別の予定 vs 実績   │
│ [棒グラフ：12ヶ月分] │ ← 横スクロール
│                    │
│ 月別の積立金額      │
│ ┌──────────────┐   │
│ │1月 予定3万 [-]3[+]│
│ │2月 予定3万 [-]3[+]│
│ │8月 予定3万 ⚠不足   │
│ │   [-]0[+]       │
│ │...              │
│ └──────────────┘   │
│                    │
│ [閉じる] [保存する] │
└────────────────────┘
```

#### 棒グラフ仕様

- 月別の縦棒2本ペア（予定：薄い青、実績：緑）
- 12ヶ月分を横並び（画面幅を超える場合は横スクロール）
- 各月の幅：32px
- バーの幅：10px
- 高さ：100px

#### 実績バーの色分け

- 予定通り（actual === plan）：**緑**（#1D9E75）
- 予定より少ない（actual < plan）：**オレンジ**（#EF9F27）← 警告
- 予定より多い（actual > plan）：**青**（#185FA5）← 余裕

#### 月別入力リスト

各行に：
1. 月名（38px幅）
2. 「予定 ◯万円」のラベル（小さくグレー）
3. ステータスラベル（不足時のみ表示）：「⚠ 3万円不足」
4. 右端に[−][数値][+]の入力UI

#### ステータスラベル

- 不足時：「⚠ ◯万円不足」（オレンジ系）
- 余剰時：「✓ ◯万円多め」（青系）
- 予定通り：表示なし

#### 行の背景色

- 不足月：薄いオレンジ背景（rgba(239, 159, 39, 0.06)）
- それ以外：白

#### ±ボタンの挙動

- 1万円単位で増減
- 0未満にはならない
- 操作するたびに：
  1. 棒グラフが更新
  2. 月別行のステータスが更新
  3. **メインの折れ線グラフの2025年の実績点が動く**

### 8-4. 支出実績ポップアップ（大型・80vh）

**タイトル：支出を記録する**

積立実績ポップアップと**完全に同じUI**を使う：
- 月別の予定vs実績の棒グラフ
- 月別の支出金額入力リスト
- ±ボタンで月別に入力

#### 違い：色分けロジックが逆

- 予定通り（actual === plan）：緑
- 予定より少ない（actual < plan）：青（節約成功）
- 予定より多い（actual > plan）：**オレンジ**（使いすぎ警告）

#### ステータスラベル

- 超過時：「⚠ ◯万円超過」
- 節約時：「✓ ◯万円少なめ」
- 予定通り：表示なし

---

## 9. リアルタイムグラフ反映

### 設計の核心

ポップアップ内で値を変更するたびに、**メインの折れ線グラフが即座に動く**。
ユーザーは「自分の操作が未来をどう変えるか」を体感できる。

### 各ポップアップでの挙動

#### 積立計画ポップアップ

月の積立額（万円/月）を変更すると：
- 適用開始年以降の計画ラインの傾きが変化
- 計画ラインの破線が動く

実装イメージ：
```javascript
function updateChartFromSavingPlan(monthlyAmount, startIdx) {
  for (var i = startIdx; i < chartData.plan.length; i++) {
    var prevValue = chartData.plan[i-1] || 0;
    var yearsGap = 2; // データの粒度
    chartData.plan[i] = prevValue + monthlyAmount * 12 * yearsGap;
  }
  drawChart();
}
```

#### 支出計画ポップアップ

支出金額を変更すると：
- 該当年の支出マーカーの位置・大きさが変化
- それ以降の計画ラインの落ち込み方が変化

#### 積立実績ポップアップ

月別の実績を ± で変更すると：
- 該当年の実績合計が変化
- メイン折れ線グラフの実績ラインの該当点が動く

実装イメージ：
```javascript
function updateMainChartFromActualSaving() {
  var total = monthData.reduce((s, m) => s + m.actual, 0);
  chartData.actual[selectedYearIdx] = total;
  drawChart();
}
```

#### 支出実績ポップアップ

積立実績と同じく、月別合計を該当年の支出として反映。

### アニメーション

- グラフ要素の transition: 0.3s 程度
- 急激な変化を避け、滑らかに動く
- 過剰なアニメーションは不要

---

## 10. 表記ルール

### 金額表示

すべて**万円単位**（1万円以上）。1万円未満は切り捨て。

| 種別 | 例 | 補足 |
|---|---|---|
| ヘッダー金額 | 430万円 | |
| カード内金額 | +5万円/月 / -72万円/年 | |
| ポップアップ入力 | 数値 + 「万円」単位ラベル | |
| グラフY軸 | 100、200、300、400 | 単位は凡例に明記 |

### 時系列表示

| 種別 | 例 |
|---|---|
| カードヘッダー | 2025年〜 / 2030年〜 / 2044年 |
| ポップアップ入力 | 数値 + 「年」単位ラベル |
| グラフX軸 | '25、'35、'45 |
| 月別UI | 1月、2月、...、12月 |

### ±ボタンの増減幅

- 月の積立額：1万円
- 支出金額（年単位）：1万円
- 支出金額（月単位）：1万円
- 月別実績：1万円

長押しで10万円単位の増減にしてもOK（実装は任意）。

---

## 11. カラー仕様

### CSSカスタムプロパティ

```css
:root {
  --bg-primary: #f5f4ee;
  --bg-card: #ffffff;
  --text-primary: #2c2c2a;
  --text-secondary: #73726c;
  --text-tertiary: #9c9a92;
  --border: rgba(0,0,0,0.08);
  --border-strong: rgba(0,0,0,0.18);
  
  /* 色の用途 */
  --plan-color: #185FA5;       /* 計画（青） */
  --actual-color: #1D9E75;     /* 実績（緑） */
  --expense-color: #E24B4A;    /* 支出（赤） */
  --milestone-color: #185FA5;  /* マイルストーン */
  --orange: #EF9F27;           /* 警告（オレンジ） */
  
  /* ハイライト */
  --select-bg: #E6F1FB;        /* カード選択時の背景 */
  --select-border: #185FA5;    /* カード選択時の枠 */
  --warn-bg: #FAEEDA;          /* 警告背景 */
  --warn-text: #9c5800;        /* 警告テキスト */
  
  /* AI */
  --ai-bg: #EAF3DE;
  --ai-color: #1D9E75;
}
```

---

## 12. 実装チェックリスト

### 画面構成

- [ ] ヘッダー：戻るボタン、PJ名、現在の積立額、目標金額
- [ ] 折れ線グラフカード（凡例 + 期間タブ + グラフ本体）
- [ ] 積立/支出タブ（緑のアクティブ表示）
- [ ] 計画/実績の2カラムヘッダー
- [ ] 年表エリア（スクロール可、横並び2カラム）
- [ ] 下部固定編集ボタン

### グラフ

- [ ] 計画ライン（青破線）
- [ ] 実績ライン（緑実線）
- [ ] 支出マーカー（赤丸 + 縦点線）
- [ ] マイルストーン（色付き円）
- [ ] 期間切替タブ（生涯/5年/1年）
- [ ] Y軸数字のみ表示、凡例横に「単位:万円」
- [ ] X軸短縮表記（'25形式）
- [ ] ハイライト機能（脈動光るアニメ）

### 操作モデル

- [ ] カードタップ → 選択状態（青背景+青枠）
- [ ] 同じカード再タップ → 選択解除
- [ ] タブ切替時 → 選択クリア
- [ ] グラフの該当年がほわほわ光る
- [ ] 下部ボタンが「○○を編集」に変化
- [ ] ボタン押下で該当ポップアップが開く

### ポップアップ

- [ ] 4種類すべて実装（積立計画/積立実績/支出計画/支出実績）
- [ ] 計画系は60vh、実績系は80vh
- [ ] 上部のグラフが見える状態を維持
- [ ] ハンドル、タイトル、サブテキスト、ボディ、アクションボタン
- [ ] バックドロップタップで閉じる

### 入力UI

- [ ] 積立計画：月額の ± 入力
- [ ] 支出計画：年/月単位切替 + 金額の ± 入力
- [ ] 積立実績：月別棒グラフ + 月別 ± 入力
- [ ] 支出実績：月別棒グラフ + 月別 ± 入力（積立実績と統一UI）

### リアルタイム反映

- [ ] 積立計画変更 → 計画ラインの傾きがリアルタイム変化
- [ ] 支出計画変更 → 支出マーカーがリアルタイム変化
- [ ] 積立実績変更 → メイングラフの実績点がリアルタイム変化
- [ ] 支出実績変更 → メイングラフの実績点がリアルタイム変化

### 棒グラフ（実績入力ポップアップ内）

- [ ] 月別の予定（薄青）vs 実績（緑）の縦棒ペア
- [ ] 横スクロール可能
- [ ] 不足月：実績バーがオレンジ
- [ ] 余剰月：実績バーが青
- [ ] 月別行に「予定 ◯万円」ラベル
- [ ] 不足/余剰時にステータスラベル表示
- [ ] 不足月の行に薄いオレンジ背景

### 表記

- [ ] 金額：すべて万円表示（1万円未満切り捨て）
- [ ] 時系列：年/月の表示
- [ ] グラフY軸は数字のみ + 凡例に単位明記

### ポップアップタイトル

- [ ] 積立計画：「いくら積み立てる？」
- [ ] 支出計画：「いつ、いくら使う予定？」
- [ ] 積立実績：「積立を記録する」
- [ ] 支出実績：「支出を記録する」

### 未入力カード

- [ ] 「+ 記録する」表示（プラスアイコン + テキスト）
- [ ] 破線枠
- [ ] グレー系の色

---

## 補足：開発時の心構え

PJ詳細画面は、ツカイドキの「**予実管理の本丸**」です。
ここでユーザーが「自分はちゃんと貯められているか」を確認し、必要なら修正する。

迷った時は以下を判断軸に：

- [ ] ユーザーは**何を確認したい**のか？（計画 or 実績 or 両者の差）
- [ ] その操作は**直感的に未来を動かせる**か？（リアルタイム反映）
- [ ] 操作ミスを防げるか？（カード選択 → 確認 → 編集の3ステップ）
- [ ] 警告の色（オレンジ）は**叱る感じになっていない**か？

ツカイドキは「ユーザーの自由意志を尊重するアプリ」。
**採点せず、見守る存在**であることを忘れずに。

---

以上がPJ詳細画面の改修指示書です。
