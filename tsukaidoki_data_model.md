# ツカイドキ データモデル定義書 v3

最終更新：2026年5月

---

## 改訂履歴

- **v3（今回）**：誤差許容モデル採用
  - PJは口座と紐づかない（支出時のみ口座を指定）
  - PJに独自の想定利回りを持たせる
  - プール金と使いみちは「2つの独立した試算視点」と定義
  - 整合性チェックは現在残高・積立合計・支出合計の3つだけ
  - ホーム画面でプール金と使いみちのズレを可視化
- v2：PJに口座を紐づける案（廃止）
- v1：初版（廃止）

---

## 概要

ツカイドキは「フルスペック会計アプリ」として設計し、無料版はそこから削る方針。

### 重要な設計思想

**「プール金」と「使いみち」は同じ資産の2つの異なる視点。長期では誤差が出るが、それを許容し見える化する。**

- プール金視点：「どこにあるお金か」（口座別）
- 使いみち視点：「何のためのお金か」（用途別）

両者の合計は現時点では一致するが、未来の試算では：
- 口座ごとの利回りで増える
- PJごとの想定利回りで増える

→ **計算ロジックが違うので、長期では合計が乖離する**。
これは欠陥ではなく、**2つの視点を見せる**という設計上の特性。

---

## 1. 全体像：6つの数字の関係

### A. プール金（口座視点）

```
A. プール金（口座視点）
   ① 残高（現在）
   ② 積立（プール金への入金）
   ③ 支出（プール金からの出金）
```

### B. 使いみち（PJ視点）

```
B. 使いみち（PJ視点）
   ④ 残高（現在）
   ⑤ 積立（PJへの割当）
   ⑥ 支出（PJからの取り崩し）
```

### 整合性の3つのルール

```
① の合計 = ④ の合計    [残高の整合：現時点でチェック]
② の合計 = ⑤ の合計    [積立の整合：月単位でチェック]
③ = ⑥                  [支出の整合：1イベント1登録で自動整合]
```

**重要：未来の残高推移については、合計が一致することを期待しない。**

---

## 2. データエンティティ定義

### 2-1. Account（口座）

プール金画面で管理される口座データ。

| フィールド | 型 | 説明 | 例 |
|---|---|---|---|
| id | string | 識別子 | "acc_001" |
| name | string | 口座名 | "証券口座" |
| sub_name | string | サブ説明 | "SBI証券・投資信託・株式" |
| current_balance | number | ① 現在残高（円） | 3,000,000 |
| annual_rate | number | 年利（%） | 5.0 |
| color | string | 表示色 | "#0C447C" |

**シナリオ間で共通**。

### 2-2. AccountSavingPlan（口座の積立計画）

口座への ② 積立 のスケジュール。

| フィールド | 型 | 説明 | 例 |
|---|---|---|---|
| id | string | 識別子 | "asp_001" |
| account_id | string | 紐づく口座 | "acc_001" |
| start_year | number | 適用開始年 | 2026 |
| start_month | number | 適用開始月（1-12） | 4 |
| end_year | number? | 適用終了年（nullなら無期限） | 2030 |
| end_month | number? | 適用終了月 | 3 |
| monthly_amount | number | 月の積立額（円） | 50,000 |

**シナリオごと独立**。

### 2-3. Project（プロジェクト/PJ）

使いみち画面で管理されるPJデータ。

| フィールド | 型 | 説明 | 例 |
|---|---|---|---|
| id | string | 識別子 | "pj_001" |
| name | string | PJ名 | "教育資金" |
| sub_name | string | サブ説明 | "2044年大学入学まで" |
| current_balance | number | ④ 現在残高（円） | 4,300,000 |
| assumed_rate | number | 想定利回り（%） | 3.0 |
| color | string | 表示色 | "#185FA5" |

**重要：assumed_rate（想定利回り）**
- PJは口座と紐づかないが、独自の想定利回りを持つ
- デフォルト値はPJの性質に応じて推奨される
- ユーザーは設定で変更可能

**想定利回りのデフォルト値（推奨）：**

| PJの種類 | 推奨利回り | 理由 |
|---|---|---|
| 老後資金（長期） | 5.0% | 長期運用前提 |
| 教育資金（中長期） | 3.0% | 中期運用想定 |
| 住宅資金 | 1.0% | 安全運用想定 |
| もしも（生活防衛） | 0.1% | 普通預金想定 |
| 車・旅行（短期） | 0.5% | 短期で取り崩すため |
| 余剰資金 | 3.0% | バランス型 |

**シナリオごと独立**。

### 2-4. ProjectSavingPlan（PJの積立計画）

PJへの ⑤ 積立 のスケジュール。**②の積立額を超えない制約あり**。

| フィールド | 型 | 説明 | 例 |
|---|---|---|---|
| id | string | 識別子 | "psp_001" |
| project_id | string | 紐づくPJ | "pj_001" |
| start_year | number | 適用開始年 | 2026 |
| start_month | number | 適用開始月 | 4 |
| end_year | number? | 適用終了年 | 2044 |
| end_month | number? | 適用終了月 | 3 |
| monthly_amount | number | 月の積立額(円) | 30,000 |

**シナリオごと独立**。

### 2-5. Expense（支出イベント）

支出は1イベント1登録で、③ と ⑥ の両方を満たす。

| フィールド | 型 | 説明 | 例 |
|---|---|---|---|
| id | string | 識別子 | "exp_001" |
| name | string | イベント名 | "保育園費用" |
| project_id | string | 取り崩すPJ ⑥ | "pj_001" |
| account_id | string | 引き落とし口座 ③ | "acc_004" |
| year | number | 発生年 | 2027 |
| month | number? | 発生月（年単位ならnull） | 5 |
| unit | "year" \| "month" | 単位 | "month" |
| amount | number | 金額（円、unitに応じた額） | 60,000 |
| is_recurring | boolean | 継続的か | true |
| end_year | number? | 終了年 | 2032 |
| end_month | number? | 終了月 | 3 |

**重要：支出登録時の口座選択**
- 支出登録のたびに、「どの口座から出すか」を選ぶ
- その時点の口座残高を見せて、残高超過の場合は警告 or ブロック
- 「車PJ 100万円 / メイン銀行から」と「車PJ 100万円 / 証券から」は別の支出として記録される

**特定月だけ金額を変えたい場合：**
- 別レコードに分ける

**シナリオごと独立**。

### 2-6. FamilyMember（家族構成）

家族メンバー情報。

| フィールド | 型 | 説明 | 例 |
|---|---|---|---|
| id | string | 識別子 | "fm_001" |
| name | string | 名前 | "太郎" |
| role | string | 役割 | "child" / "spouse" / "self" / "pet" / "other" |
| birth_year | number | 生年 | 2024 |

**シナリオごと独立**。

### 2-7. Scenario（シナリオ）

シナリオごとに②③④⑤⑥は独立。①は共通。

| フィールド | 型 | 説明 |
|---|---|---|
| id | string | 識別子 |
| name | string | シナリオ名 |
| is_active | boolean | 選択中か |
| account_saving_plans | AccountSavingPlan[] | ②のリスト |
| projects | Project[] | プロジェクトリスト |
| project_saving_plans | ProjectSavingPlan[] | ⑤のリスト |
| expenses | Expense[] | 支出イベントリスト |
| family_members | FamilyMember[] | 家族構成 |

---

## 3. 計算ロジック

### 3-1. 月別の②積立合計

```
total_account_saving(Y, M) = 
  Σ asp.monthly_amount 
  for asp in AccountSavingPlan 
  where asp.start <= (Y,M) <= asp.end
```

### 3-2. 月別の⑤積立合計

```
total_project_saving(Y, M) = 
  Σ psp.monthly_amount 
  for psp in ProjectSavingPlan 
  where psp.start <= (Y,M) <= psp.end
```

### 3-3. ②と⑤の整合性チェック

```
任意の(Y, M)で：
  total_account_saving(Y, M) >= total_project_saving(Y, M)
```

満たされない場合 = 超過状態 → UIでブロック。

### 3-4. プール金の残高推移計算（口座別）

任意の年月 `(Y, M)` における、各口座の残高：

```
balance(account, Y, M) = 
  account.current_balance
  + Σ (monthly_saving × months)  // ②の累積
  + Σ compound_interest          // 複利（account.annual_rate）
  - Σ expense                    // ③の累積（この口座から出る支出）
```

複利計算（月次）：
```
balance *= (1 + account.annual_rate / 100 / 12)
```

### 3-5. PJの残高推移計算（PJ別）

任意の年月 `(Y, M)` における、各PJの残高：

```
balance(project, Y, M) = 
  project.current_balance
  + Σ (monthly_saving × months)  // ⑤の累積
  + Σ compound_interest          // 複利（project.assumed_rate）
  - Σ expense                    // ⑥の累積（このPJから取り崩される支出）
```

**重要：複利計算には、PJ自身の `assumed_rate` を使う：**
```
balance *= (1 + project.assumed_rate / 100 / 12)
```

### 3-6. プール金合計と使いみち合計の乖離

```
total_pool(Y, M) = Σ balance(account, Y, M) for all accounts
total_usage(Y, M) = Σ balance(project, Y, M) for all projects

divergence(Y, M) = total_pool(Y, M) - total_usage(Y, M)
```

この divergence は時間とともに拡大する可能性がある。
ホーム画面のグラフで両者を並列表示することで、ユーザーは「ズレ」を視覚的に確認できる。

### 3-7. 赤字検知

```
任意のPJで、いずれかの (Y, M) で：
  balance(project, Y, M) < 0
  → このPJは赤字PJ

任意の口座で、いずれかの (Y, M) で：
  balance(account, Y, M) < 0
  → この口座は支出超過
```

赤字検知のタイミング：
- データ変更時
- 折れ線グラフ表示時

---

## 4. 制約と整合性ルール

### 4-1. 現在残高の整合

```
Σ Account.current_balance = Σ Project.current_balance
```

ズレた場合 → ホーム画面の整合バナーで警告（既存仕様）。

### 4-2. 積立の整合（月単位）

```
任意の(Y, M)で：
  total_account_saving(Y, M) >= total_project_saving(Y, M)
```

超過しないよう、⑤の入力時にUIでブロック。

### 4-3. 支出時の口座残高チェック

支出イベント登録時：
1. 該当年月の引き落とし口座の残高を計算
2. 支出額が残高を上回らないか確認
3. 上回る場合：警告またはブロック

**支出登録UI（後で詳細設計）：**
- 「2030年に車100万円を出します」
- 「その時点の口座残高を見せる」
  - 証券口座：540万円
  - 定期預金：210万円
  - 積立NISA：680万円
  - メイン銀行：80万円
  - サブ銀行：50万円
- 「どこから出しますか？」（複数選択可）

### 4-4. 赤字回避

```
全PJで、全期間で balance >= 0
全口座で、全期間で balance >= 0
```

赤字が発生したら警告（叱らないトーンで）。

---

## 5. シナリオの扱い

### 5-1. シナリオ間で共通のデータ

- Account（口座定義、年利、現在残高）
- 設定（インフレ率、退職年齢など）

### 5-2. シナリオごとに独立のデータ

- ② AccountSavingPlan
- Project（PJ定義、現在残高、想定利回り）
- ⑤ ProjectSavingPlan
- 支出イベント Expense
- 家族構成 FamilyMember

### 5-3. シナリオ切替時の挙動

1. ロードオーバーレイ表示
2. 該当シナリオのデータを読み込み
3. 全画面を再計算
4. ホーム画面に遷移

---

## 6. 計算結果のキャッシュ戦略

### 6-1. 計算粒度

- ホーム画面の長期グラフ：**年単位**
- 使いみち画面の長期グラフ：**年単位**
- プール金画面の長期グラフ：**年単位**
- PJ詳細画面の折れ線：**年単位**（5年表示なら月単位）
- 整合性チェック用：**月単位**

### 6-2. キャッシュ

- データ変更時：該当シナリオのキャッシュを無効化
- 再計算は遅延実行

---

## 7. 設定値（上級者向け）

設定画面で変更可能：

| 設定 | デフォルト | 説明 |
|---|---|---|
| 利回り（口座別） | 各口座のannual_rate | 複利計算（プール金側） |
| 利回り（PJ別） | 各PJのassumed_rate | 複利計算（使いみち側） |
| インフレ率 | 1.0%/年 | 将来の支出予測（任意） |
| 退職年齢 | 65歳 | 老後資金計算の基準 |
| 平均寿命 | 90歳 | 老後資金計算の基準 |

無料版では固定値、有料版でカスタマイズ可能。

---

## 8. オンボーディングフロー

```
1. ウェルカム
2. 家族構成入力
3. 口座登録（①残高、年利、月の積立額）
4. 興味カード選択（PJ候補）
5. PJごとの質問（公立/私立、相場金額など）
6. AI自動計算
   - PJの assumed_rate をデフォルト推奨値で設定
   - PJの現在残高を自動振り分け（②の累積額に基づく）
   - PJの積立額を自動配分（②の月額に基づく）
   - 支出イベントの引き落とし口座を自動推奨
7. 確認画面（自動計算結果を見せる）
   - 「これは2つの異なる視点の試算です」と明示
8. 完成 → 使いみち画面に着地
```

---

## 9. プール金と使いみちの「ズレ」の見せ方

### ホーム画面

```
┌──────────────────────────┐
│ 総資産 1037万円           │
├──────────────────────────┤
│ プール金 vs 使いみちの推移 │
│ [折れ線グラフ：両方を重ねて表示] │
│  ─ プール金（口座視点）    │
│  ─ 使いみち（PJ視点）      │
│  両者の乖離が見える        │
├──────────────────────────┤
│ ✦ AIインサイト           │
│ 「これは2つの異なる視点の  │
│  試算です。差は運用方法の  │
│  違いから生まれます」     │
├──────────────────────────┤
│ 今年の支出予定            │
│ ・5月 保育園 -6万円       │
│ ・8月 旅行 -30万円        │
└──────────────────────────┘
```

### ユーザーへの説明

オンボや初回利用時に：
```
✦ ツカイドキの仕組み

ツカイドキは、あなたのお金を
「どこにあるか（プール金）」と
「何のためか（使いみち）」の
2つの視点で試算します。

長期では、運用方法の違いで
両者に誤差が出ますが、
これは正常な状態です。

2つの視点を比べることで、
あなたの未来がより立体的に
見えてきます。
```

---

## 10. データの完全性

### 必須データ

新規ユーザーがオンボ完了時点で持つもの：
- Account ≥ 1
- AccountSavingPlan ≥ 0
- Project ≥ 1（assumed_rate必須）
- ProjectSavingPlan ≥ 0
- Expense ≥ 0
- Scenario = 1
- FamilyMember ≥ 1（本人）

### 削除時のカスケード

- Project削除 → 紐づくProjectSavingPlan、ExpenseのProject参照を削除/警告
- Account削除 → 紐づくAccountSavingPlan、ExpenseのAccount参照を削除/警告
- Scenario削除 → 紐づく②⑤Expense、FamilyMemberは削除

---

以上がデータモデル定義書 v3 です。
