# ツカイドキ カラーシステム実装指示書

## 0. 設計思想

ツカイドキのカラーシステムは **3層構造** で設計する。

```
┌─────────────────────────────────────────────┐
│  Layer 1: ブランド層                        │
│  アプリ全体のアイデンティティ。固定。       │
│  例：sage（メイン）、honey（警告）          │
├─────────────────────────────────────────────┤
│  Layer 2: 軸層（プール金 / 使いみち）       │
│  2つの管理体系を視覚的に区別する。          │
│  プール金 = 単一トーン（青）                │
│  使いみち = ジュエルトーンパレット（多色）  │
├─────────────────────────────────────────────┤
│  Layer 3: グレースケール層                  │
│  背景・テキスト・区切りなど構造要素。        │
└─────────────────────────────────────────────┘
```

**重要：個別カテゴリ（教育資金、車資金など）に色を固定割り当てしない。**
色はパレット内のスロットとして定義し、ランタイムにカテゴリへ動的割り当てする。

---

## 1. Layer 1: ブランド層（固定）

```ts
export const brand = {
  // メインアクセント：CTA、選択中タブ、アクティブ状態
  sage: {
    base: '#5B8E7D',
    light: '#8BB0A2',
    bg: '#E8EFEB',          // バッジ背景、ピル背景
  },

  // 警告・注目：アラート、要修正
  honey: {
    base: '#E8B86D',
    dark: '#B8893E',         // 警告テキスト用
    bg: '#FBF1DD',          // 警告バナー背景
  },
};
```

**原則：**
- これらの色は**用途が固定**されている。流用しない
- sage = ブランドの「中心軸」。CTA、ホームタブのアクティブ、AIインサイトのアクセント
- honey = 「注意を引きたいが、優しく」。差額、要注意、未達成
- **絶対に純粋な赤（#FF0000系）を使わない**

---

## 2. Layer 2: 軸層（プール金 / 使いみち）

ツカイドキの根幹である「2つの管理体系」を**色のトーンで対比させる**。

### 2.1 プール金カラー（単一トーン・グラデーション）

**設計思想：「整然・統一・構造」**
- プール金は「資産の保管場所」=同質なものの集合
- 単一の色相（青）の濃淡だけで構成
- 金額の大きさと色の濃さが比例するように動的にソート

```ts
export const poolPalette = {
  // 5段階の青トーン（濃→淡）
  // 必要に応じて補間して N 段階にも拡張可能
  tones: [
    '#2E5F8A',   // tone-1: 最も濃い（最大金額の口座）
    '#4A7FA8',   // tone-2
    '#6B9BC4',   // tone-3
    '#95B8D4',   // tone-4
    '#C2D5E5',   // tone-5: 最も淡い（最小金額の口座）
  ],
};
```

**動的割り当てロジック：**

```ts
function assignPoolColors(accounts: Account[]): Map<AccountId, string> {
  // 金額の降順でソート
  const sorted = [...accounts].sort((a, b) => b.amount - a.amount);

  // トーンを順番に割り当て
  const tones = poolPalette.tones;
  const result = new Map();
  sorted.forEach((account, i) => {
    // 口座数がトーン数を超える場合はループまたは補間
    const tone = tones[Math.min(i, tones.length - 1)];
    result.set(account.id, tone);
  });
  return result;
}
```

**口座数がトーン数（5）を超える場合の戦略：**
- オプションA：HSLで補間して N 段階を生成
- オプションB：6個目以降は最も淡いトーンにまとめる
- オプションC：「他N件」として集約表示
- 推奨：**オプションA（HSL補間）**。例えば10口座なら HSL の Lightness を等分割して10段階生成

```ts
function generatePoolTones(count: number): string[] {
  // 基準色 #4A7FA8 を中心に、Lightnessを変化させて N 段階生成
  // HSL: hue=210, sat=40%, lightness 25%〜85% の範囲で等分
  const hue = 210;
  const sat = 40;
  const lMin = 25;
  const lMax = 85;
  return Array.from({ length: count }, (_, i) => {
    const l = lMin + (lMax - lMin) * (i / Math.max(count - 1, 1));
    return `hsl(${hue}, ${sat}%, ${l}%)`;
  });
}
```

### 2.2 使いみちカラー（ジュエルトーン多色パレット）

**設計思想：「彩り・多様性・夢の豊かさ」**
- 使いみちは「人生の目的」=多様な意図の集合
- ジュエルトーン（彩度高めだが、くすみあり）の多色パレット
- 色相を等間隔に配置し、視覚的に区別しやすく

```ts
export const usePalette = {
  // 6色のジュエルトーンパレット
  // カテゴリ非依存。スロットとして定義
  jewels: [
    '#2E6FB8',   // jewel-1: ロイヤルブルー
    '#3D9670',   // jewel-2: フォレストグリーン
    '#E07142',   // jewel-3: テラコッタオレンジ
    '#9376C9',   // jewel-4: アメジスト
    '#D4A847',   // jewel-5: マスタードゴールド
    '#C56A9E',   // jewel-6: ローズピンク
  ],

  // 「余剰資金」「未分類」など、特別な意味を持つカテゴリ用
  // ジュエルトーンとは別系統のニュートラル
  neutral: '#A8AFB3',
};
```

**動的割り当てロジック：**

```ts
function assignUseColors(purposes: Purpose[]): Map<PurposeId, string> {
  const jewels = usePalette.jewels;
  const result = new Map();

  // 「余剰資金」「未分類」などのシステム定義カテゴリは neutral へ
  // それ以外はユーザー作成順 or 並び順で jewels から順番に割り当て
  let jewelIndex = 0;
  for (const purpose of purposes) {
    if (purpose.isSystemCategory && purpose.type === 'surplus') {
      result.set(purpose.id, usePalette.neutral);
    } else {
      const color = jewels[jewelIndex % jewels.length];
      result.set(purpose.id, color);
      jewelIndex++;
    }
  }
  return result;
}
```

**用途数がパレット数（6）を超える場合の戦略：**
- 推奨：**色相をローテーション**（7個目はjewel-1の少し明度違い、など）
- 並び順を変えても色が変わらないよう、**カテゴリIDでハッシュして決定的に割り当てる**方法もアリ

```ts
function assignUseColorsByHash(purposes: Purpose[]): Map<PurposeId, string> {
  // ハッシュベース：同じカテゴリには常に同じ色
  // 並び替えても色が変わらない
  const result = new Map();
  for (const purpose of purposes) {
    if (purpose.isSurplus) {
      result.set(purpose.id, usePalette.neutral);
      continue;
    }
    // IDのハッシュ値からインデックスを決定
    const hash = simpleHash(purpose.id);
    const color = usePalette.jewels[hash % usePalette.jewels.length];
    result.set(purpose.id, color);
  }
  return result;
}
```

**推奨アプローチの選択：**
- **新規作成時に color スロットを保存する**設計が最もシンプル
  - DB スキーマに `purpose.colorIndex: number` を持つ
  - 作成時に「未使用のスロット」を自動割り当て、または「使用中スロットの最少使用回数」を採用
  - ユーザーが手動で色を変えたければ後から変更可能
  - **最も予測可能で、ユーザー体験が一貫**

---

## 3. Layer 3: グレースケール層

```ts
export const neutral = {
  bg: '#FAF7F2',           // アプリ背景（クリーム）
  card: '#FFFFFF',          // カード背景
  cardAlt: '#FFFEFB',       // カードのバリエーション
  divider: '#EDE8DF',       // 区切り線、進捗バーの未達成部分

  text: {
    primary: '#2C3539',     // 本文・見出し（純黒禁止）
    mid: '#6B7378',         // 補助テキスト
    light: '#A0A6A9',       // ラベル、メタ情報
  },
};
```

---

## 4. 使用箇所マッピング

カラーがどこに使われるかの定義：

### プール金カラー（poolPalette）
- プール金画面：口座リストの左カラーバー
- プール金画面：エリアグラフのセグメント
- ホーム画面：プール金カードの円グラフセグメント
- ホーム画面：プール金カードの凡例ドット

### 使いみちカラー（usePalette）
- 使いみち画面：上部の積み上げ棒グラフセグメント
- 使いみち画面：凡例ドット
- 使いみち画面：各夢カードの左カラーバー
- 使いみち画面：各夢カードの進捗バー塗り
- ホーム画面：使いみちカードの円グラフセグメント
- ホーム画面：使いみちカードの凡例ドット

### ブランドカラー（brand）
- sage：ホームタブのアクティブ、CTA、「順調」バッジ、AIインサイト、選択中ピル
- honey：「要注意」バッジ、差額バッジ、警告アラート、未達成ハイライト

---

## 5. 円グラフ・棒グラフ実装の補足

### 5.1 セグメント間の白い間仕切り

各セグメントの境界に幅2pxの薄い白線（`rgba(255, 255, 255, 0.85)`）を入れる。
SVG の場合、各セグメントの開始位置に短い白いストロークを追加する手法。

理由：
- セグメント間の認識が容易になる
- 特にプール金（青グラデ）で隣接トーンが似ている場合に必須
- 純白ではなく85%透明度にすることで、カード背景に馴染む

### 5.2 プール金の色順序

プール金グラフは**金額の降順**で並べる（最大が最濃→最小が最淡）。
これにより「色の濃さ＝重要度」という直感的な視覚マッピングが成立する。

### 5.3 使いみちの色順序

使いみちは**カテゴリ作成順 or ユーザー定義の並び順**で割り当てる。
金額順ではない理由：用途は「人生の彩り」なので、サイズの大小ではなく**並び順そのもの**に意味がある（ユーザーの優先順位など）。

---

## 6. ダークモード対応（将来）

この時点では実装不要だが、構造として `light` / `dark` でトークンを分岐できる設計にしておく。

```ts
export const colors = {
  light: {
    brand: { sage: { ... }, honey: { ... } },
    pool: { tones: [...] },
    use: { jewels: [...], neutral: '#A8AFB3' },
    neutral: { bg: '#FAF7F2', ... },
  },
  dark: {
    // 将来定義
  },
};
```

ダークモード時は：
- 背景は深い茶系（#1C1A16 など）
- カード背景は少し明るい茶（#2A2620 など）
- ジュエルトーンは少し明度を上げる
- プール金グラデーションは反転（淡→濃）

---

## 7. 実装チェックリスト

- [ ] poolPalette と usePalette を CSS変数 or デザイントークンとして定義
- [ ] ハードコードされた色（#XXXXXX）を全て廃止
- [ ] プール金は金額降順で色を割り当てるロジック実装
- [ ] 使いみちは作成順 or 並び順で色を割り当てるロジック実装
- [ ] 余剰資金など特別カテゴリは neutral へマッピング
- [ ] 円グラフ・棒グラフに白い間仕切り（rgba(255,255,255,0.85)、2px）を実装
- [ ] sage と honey の用途を厳密に守る（流用しない）
- [ ] 純粋な赤を一切使わない
- [ ] パレット数を超える場合の挙動（補間 or ループ or 集約）を決定して実装

---

## 8. なぜこの設計なのか（開発者向けメモ）

**なぜプール金は単一トーン？**
プール金は「資産の保管場所」という同質なものの集合。**「整然」「構造」「信頼**」を視覚で語るため、色相を絞って濃淡だけで差をつける。これは銀行や証券会社の伝統的な配色とも近い。

**なぜ使いみちは多色？**
使いみちは「人生の目的」という多様な意図の集合。**「彩り」「夢」「冒険心」**を視覚で語るため、色相を分散させたジュエルトーンを使う。これはツカイドキのビジョン「希望」「冒険心」と直結している。

**なぜジュエルトーン（くすみ多色）？**
ピュアなビビッドカラーは「ゲーム」「子供向け」のニュアンスが強く、ツカイドキのターゲット（30〜50代、中間所得層）には軽すぎる。彩度を一段落としたジュエルトーンなら、**大人の彩り**として成立し、トーン＆マナーの「静謐」「押し付けない」とも矛盾しない。

**なぜカテゴリに色を固定しない？**
ユーザーが新しい夢を追加・削除・並び替えする以上、固定割り当ては破綻する。カラーは**スロットとして定義**し、カテゴリインスタンスにランタイムまたは作成時に割り当てる。これにより、ユーザーごとに違う組み合わせ・違う色順序になっても破綻しない。
