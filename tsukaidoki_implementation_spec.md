# ツカイドキ 実装指示書（完成版）

最終更新：2026年4月

---

## 目次

1. [ビジョン・思想](#1-ビジョン思想)
2. [アプリ概要](#2-アプリ概要)
3. [コア用語](#3-コア用語)
4. [操作モデル](#4-操作モデル)
5. [タブ構成](#5-タブ構成)
6. [ホーム画面](#6-ホーム画面)
7. [使いみち画面](#7-使いみち画面)
8. [プール金・口座管理画面](#8-プール金口座管理画面)
9. [PJ詳細画面](#9-pj詳細画面)
10. [夢年表画面](#10-夢年表画面)
11. [シナリオタブ](#11-シナリオタブ)
12. [オンボーディング](#12-オンボーディング)
13. [AI仕様](#13-ai仕様)
14. [ゲーミフィケーション設計](#14-ゲーミフィケーション設計)
15. [通知設計](#15-通知設計)
16. [マネタイズ](#16-マネタイズ)
17. [デザイン原則](#17-デザイン原則)
18. [Claude Codeへの指示](#18-claude-codeへの指示)

---

## 1. ビジョン・思想

### ビジョン

> 暗中模索、どれだけお金を貯めたらよいのか、はたまたどれだけ人生の道草食っていいのやらの霧を晴らす地図となる。
> 地図が見えることで、人生のゴール（安心の死）にむけて、安全なルートで、かつ多いに道草を食えるという、安心と希望と冒険心と自信をユーザーに与える。

### アプリの立ち位置

- **誠実**であり、人生の価値観は人それぞれという思想
- 人生を**健全に楽しむ**一助になること
- ただし**押し付けがましくあってはいけない**
- ライフマネープランは人生の縮図であり、客観視で人生を見つめ直す機会を提供
- **カーナビの推奨ルート**のように提案だけにとどまり、目的地・ルート選択はユーザーが決める

### 4つの感情の設計

ツカイドキは、ユーザーに以下の4つの感情を与える設計とする：

| 感情 | 内容 |
|---|---|
| **安心** | 老後・教育・もしもの事態でも生きていけるかへの答え |
| **希望** | 自分にも車を買ったり海外旅行に行く未来があるという感覚（余力時、AIが事前提示） |
| **冒険心** | 保守的な人生から、もう一歩踏み出して試してみる勇気 |
| **自信** | 利用前と比べて、苦手な財務管理ができている実感 |

### 思い出機能を持たない

ツカイドキはカーナビ的存在であり、過去の振り返り機能を持たない。
**常に「現在と未来の関係」だけ**を映す。

---

## 2. アプリ概要

### コンセプト

**「未来の試算を、夢から逆算して描くアプリ」**

- 家計簿（過去の記録）でもない
- 計画書（縛り）でもない
- 中間所得層が、人生の節目で頼る相談相手
- 大きな買い物の前に開く、未来を見るアプリ

### ターゲット

- 中間所得層
- 30〜50代、世帯月収40〜60万円程度
- 「貯まってる実感がほしい」「将来が不安」という悩みを持つ

### 既存アプリとの差別化

| 既存アプリ | 主軸 | ツカイドキ |
|---|---|---|
| マネーフォワード | 過去の集計 | 未来の試算 |
| Zaim | 家計簿 | 夢からの逆算 |
| FP相談 | 専門家相談 | スマホで完結 |
| あすけん | 食事記録の採点 | 採点しない、見守る |

---

## 3. コア用語

実装時、画面上のテキストはこの用語で統一する。

| 用語 | 意味 |
|---|---|
| **プール金** | 実際の口座残高（自動連携 or 手入力）。財務会計の概念 |
| **ポートフォリオ** | 用途別に仕分けた試算。管理会計の概念 |
| **プロジェクト（PJ）** | 用途別バケツ（教育・老後・車・旅行など） |
| **使いみち** | プロジェクトの一覧・俯瞰画面 |
| **夢年表** | 時系列にプロットした★（やりたいこと） |
| **試算** | 全データから自動で出る未来予測 |
| **シナリオ** | 試算の入れ物（プランA / プランB ...） |
| **積立ルール** | 月いくら、いつから、いつまで（自動積立の設定） |
| **出来事** | いつ、いくら使う/もらう（過去・未来問わず） |
| **AIインサイト** | 状況を語るAIのコメント。✦マークがシグネチャ |
| **生活防衛費** | もしもの事態に備える金額（一般的な目安をデフォルト） |

### 廃止する用語（使わない）

- ❌ 計画 / 予定
- ❌ 実績
- ❌ シミュレーション（→ 試算に統一）
- ❌ 配分 / 振替（→ 残高を直接書き換える）
- ❌ 記録（→ 出来事として登録）
- ❌ 健全度 / 達成率%
- ❌ ヨミちゃん等のキャラクター名

---

## 4. 操作モデル

### 入力できるのは4種類だけ

| 項目 | 内容 | 入力タイミング |
|---|---|---|
| **残高** | プール金 / 各PJ残高 | 自動連携 or 手入力 |
| **積立ルール** | 月いくら、いつから、いつまで | 各PJ画面で設定 |
| **出来事** | いつ、いくら使う/もらう | 各PJ画面で追加 |
| **夢（★）** | いつ、何を、どのPJで（金額なし） | 夢年表画面で追加 |

### 重要な設計原則

1. **すべては試算**：過去も未来もフラットに扱う。「予定vs実績」の区別なし
2. **目標vs試算の二軸**：夢年表で目標、各PJで試算。両者の差を見せる
3. **金額は直接書き換える**：振替・配分という概念は廃止
4. **支出も計画として登録**：「もし車買ったら」も「実際に車買った」も同じ操作

### 整合性ルール

**プール金の合計 ＝ ポートフォリオ各PJ残高の合計**

ズレた場合、ホーム画面の整合バナーで通知。ユーザーが残高を直接編集して整合させる。

---

## 5. タブ構成

最下部のタブは5つ。

```
[ 📊 シナリオ ] [ 🏦 プール金 ] [ 🏠 ホーム ] [ ✨ 使いみち ] [ ⚙ 設定 ]
```

- **シナリオタブ（最左）**：シナリオ切替・新規作成・課金導線
- **プール金**：口座残高管理・積立/複利調整・将来残高グラフ
- **ホーム**：情緒の集約地（安心・自信・希望）
- **使いみち**：全PJ一覧 + AIインサイト + 積立金調整
- **設定**：プロフィール・通知・連携・ヘルプ

夢年表とPJ詳細はタブには含まれない。ホームや使いみちから遷移する。

> **注：** 管理画面（/manage）はタブ非表示だが実装済み。設定画面から遷移。

---

## 6. ホーム画面

### レイアウト構造

スクロールせず1画面に収める。情報を絞り、密度を高める。

```
┌──────────────────────────┐
│ 🌱 ツカイドキ        ☰  │ コンパクトヘッダー
│ 📊 プランA：メイン       │ シナリオ表示（タップでシナリオタブ）
├──────────────────────────┤
│ 総資産                   │
│ ¥7,750,000              │
│ 8PJ・12夢・4口座 管理中  │ 自信表現（1行・控えめ）
├──────────────────────────┤
│ ✓ プール金 = ポートフォリオ│ 整合バナー
│   一致 / 差額 ¥0          │
├──────────────────────────┤
│ ┌─────────┬─────────┐    │
│ │プール金  │使いみち  │    │ 円グラフ × 2
│ │ 円グラフ │ 円グラフ │    │ （合計表示行は不要）
│ └─────────┴─────────┘    │
├──────────────────────────┤
│ ✦ AIインサイト           │
│ [安心 or 希望のメッセージ]│
└──────────────────────────┘
```

### AIインサイトの切り替えロジック

ユーザーの状況によって、表示内容が変わる。

#### パターン1：1つでも△があれば → 安心系メッセージ

主要PJ（老後・教育・もしも・住宅・介護のうち登録されているもの）に1つでも未達があれば表示。

```
✦ AIインサイト

老後資金の達成が8年遅れ気味です
月+¥15,000で計画通りになります

教育資金、もしもの備えは
順調に積み上がっています
```

#### パターン2：全部✓なら → 希望系メッセージ

```
✦ AIインサイト

備えはバッチリです
余力で何ができるか、見てみますか？

¥350万のSUVが買えます
家族でハワイ旅行に行けます

[ 試算してみる ]
```

### AIインサイトの仕様

- **更新タイミング**：データ変更時のみ（積立調整、夢追加、シナリオ切替、残高更新、給料日）
- **長さ**：3〜5文
- **アクションボタン**：希望系の「試算してみる」のみ。安心系は事実と提案のみで、ボタンは置かない
- **トーン**：押し付けない、見守る

### 円グラフの仕様

- **左：プール金（口座別）** — 5口座が異なる青系で塗り分け
- **右：使いみち（用途別）** — PJごとに固有色
- 中央に合計金額と件数（「¥825万 / 5口座」など）
- タップで一覧画面に遷移
- **合計行（プール金 合計、ポートフォリオ 合計、差額）は表示しない**

### シナリオセレクター

- 上部に「📊 プランA：メイン」と表示のみ
- タップでシナリオタブに遷移
- ホームではプルダウン等で切り替えしない

---

## 7. 使いみち画面

### レイアウト構造

```
┌──────────────────────────┐
│ 🌱 ツカイドキ  総資産    │ ヘッダー（右に総資産金額）
│              ¥7,750,000 │
│ ライフマネープラン        │
├──────────────────────────┤
│ ▓▓▓▓▓▓▓░░░ 配分バー      │ ← 全PJの比率を1本のバーで
│ ● 教育 49% ● 老後 35%    │
│ ● 車 14%  ● 旅行 1%      │
├──────────────────────────┤
│ ✦ AIインサイト           │ 3〜5文の解説
│ 12の夢のうち、8つは現状   │ 変更時のみ更新
│ ペースで届きます。        │ アクションボタンなし
│ 老後PJの達成が8年遅れ気味 │
│ 月+¥15,000で計画通りに。  │
├──────────────────────────┤
│ [⚙ 積立金調整 ∨]         │ ← 開閉トグル
│（展開時：AllocationPanel）│
├──────────────────────────┤
│ [カスタム][緊急順][時期順]│ ← 並び替えタブ
├──────────────────────────┤
│ [PJカード] 老後資金 △要注意│
│ [PJカード] 教育資金 ✓順調 │
│ [PJカード] 車資金 ✓順調   │
│ [PJカード] 旅行資金 ✓順調 │
└──────────────────────────┘
```

### ヘッダー

- 左：Logo（ツカイドキアイコン）
- 右：「総資産」ラベル + `¥X,XXX,XXX` 金額（白文字）
- 下段：「ライフマネープラン」サブタイトル

### 配分バー（AllocationBar）

- 全PJの残高比率を横一本のバーで表示
- 余剰資金はグレーで末尾に追加
- 凡例：各PJの名前と%を2列で表示
- 総資産の数値はバー内には表示しない（ヘッダーに表示済みのため）
- 夢年表を見るボタンは持たない

### 積立金調整パネル（AllocationPanel）

「⚙ 積立金調整」ボタンで展開/折りたたみ。

```
┌──────────────────────────┐
│ 月の積立配分 合計 ¥○○/月 │
│ ▓▓░░░░░ 配分比率バー      │
│ ● 教育 55% ● 老後 45%    │
│                          │
│ 各PJ調整（月積立 / 残高） │
│ ●老後  残高 ¥2.5M ✎      │ ← タップで残高インライン編集
│        [−] 30,000 [+]    │ ← 月積立ステッパー
│ ●教育  残高 ¥3M ✎         │
│        [−] 20,000 [+]    │
│ ...                      │
│                          │
│ ✦ AI自動配分調整          │
│ 老後PJの達成が遅れています │
│ 月+¥15,000の増額を提案    │
│ [ 提案を適用 ]            │
│                          │
│ [    適用する    ]        │
└──────────────────────────┘
```

#### 残高インライン編集

- 残高テキストの右に ✎ アイコンを表示
- タップで残高テキストが TextInput に切り替わる（アンダーライン表示）
- `¥` プレフィックス + 数値入力（number-pad）
- フォーカスが外れると（onBlur）自動確定・保存

#### 月積立ステッパー

- `−` / `＋` ボタンで ¥1,000 刻み
- 中央の TextInput で直接入力可能
- 負の値にはならない

#### AI自動配分調整カード

- warn ステータスの PJ がある場合に表示
- 月 +¥15,000 ずつの増額を提案
- 「提案を適用」ボタンで localMonthly に反映（まだ保存されない）
- 全 PJ が ok の場合は「現在の配分は最適です」と表示

### PJカード

各カードに：
- PJ名
- ステータス（✓順調 / △要注意）— ✗は使わない
- 残高
- プログレスバー（夢に対する達成度）

タップでPJ詳細画面に遷移。カスタムモードでは長押しでドラッグ並び替え可能。

---

## 8. プール金・口座管理画面

### レイアウト構造

```
┌──────────────────────────┐
│ 🌱 ツカイドキ   総残高   │ ヘッダー（右に総残高金額）
│              ¥7,750,000 │
│ プール金 · 口座管理       │
├──────────────────────────┤
│ ┌──────────────────────┐ │
│ │ 将来残高グラフ        │ │ 積み上げ面積グラフ（生涯）
│ │ ■証券 ■定期 ■NISA...  │ │ 凡例つき
│ └──────────────────────┘ │
├──────────────────────────┤
│ 口座残高         タップで │ セクションヘッダー
│                  残高修正 │
│                          │
│ ■ 証券口座   ¥3,000,000 ✎│ ← タップで残高インライン編集
│   証券・株式   年利 5.0%  │
│                月¥50,000  │
│                          │
│ ■ 定期預金   ¥2,500,000 ✎│
│   定期預金     年利 0.5%  │
│ ...                      │
├──────────────────────────┤
│ [ ⚙ 積立・複利を調整する ]│ ← 下部固定ボタン
└──────────────────────────┘
```

### ヘッダー

- 左：Logo
- 右：「総残高」ラベル + `¥X,XXX,XXX` 金額（白文字）
- 下段：「プール金 · 口座管理」サブタイトル

### 将来残高グラフ（積み上げ面積グラフ）

- 口座ごとに色分けした積み上げ面積グラフ
- X軸：現在〜生涯末（誕生年+100歳まで）
- Y軸：万/千万/億 単位の自動スケーリング
- 凡例を上部に表示

### 口座残高リスト

各行に：
- 左端：口座色のカラーバー（4px幅）
- 口座名 + メタ情報（口座種別）
- 右側：残高（タップでインライン編集） + 鉛筆アイコン
- 残高下：年利 x.x% と月積立額（設定済みの場合）

#### 残高インライン編集

- 金額右の ✎ アイコンをタップで編集モードへ
- `¥` プレフィックス + TextInput（underline スタイル）
- `onBlur` / `onSubmitEditing` で確定
- 変更がある場合 `updateBalance()` をコールし履歴記録

### 積立・複利調整モーダル

「⚙ 積立・複利を調整する」ボタンで全画面モーダルを開く。

```
┌──────────────────────────┐
│ 積立・複利を調整する  [✕]│ ヘッダー
├──────────────────────────┤
│ [生涯] [5年] [1年]        │ ← 時系列範囲セレクター
├──────────────────────────┤
│ ┌──────────────────────┐ │
│ │ 折れ線グラフ（試算）  │ │ リアルタイムプレビュー
│ │ ■証券 ■定期 ■NISA... │ │ （積み上げ折れ線）
│ └──────────────────────┘ │
├──────────────────────────┤
│ [スクロール可能エリア]    │
│                          │
│ 適用期間                 │
│ From         〜  To      │
│ [‹] 2026 [›]    [‹] 2036 [›]│
│                          │
│ 各口座の設定             │
│ ┌──────────────────────┐ │
│ │ ■ 証券口座            │ │
│ │ 月次積立額  複利（年利）│ │ ← 横並び
│ │ [−]50000[+] [−]5.0[+] │ │
│ │ ¥50,000/月  5.0%/年   │ │
│ └──────────────────────┘ │
│ ┌──────────────────────┐ │
│ │ ■ 定期預金            │ │
│ │ 月次積立額  複利（年利）│ │
│ │ [−]  0  [+] [−]0.5[+] │ │
│ └──────────────────────┘ │
│ ...                      │
├──────────────────────────┤
│ [    適用する    ]        │ ← 下部固定ボタン
└──────────────────────────┘
```

#### 時系列範囲セレクター

| 選択肢 | グラフ表示範囲 |
|---|---|
| 生涯 | 現在〜生涯末（誕生年+100歳） |
| 5年 | 現在〜5年後 |
| 1年 | 現在〜1年後 |

グラフの表示範囲のみ変更。試算ロジックには影響しない。

#### 適用期間（From / To）

- From：設定を開始する年（デフォルト：現在年）
- To：設定を終了する年（デフォルト：現在年+10）
- From ≤ To の制約あり

#### 月次積立額・複利の横並びレイアウト

各口座ブロック内で月次積立額と複利（年利）を横2列に並べる。
- 各フィールドに `−`/`＋` ステッパーボタン + TextInput
- 月次積立：¥1,000刻み、最小¥0
- 複利（年利）：0.1%刻み（内部は0.001）、最大20%
- 入力値は即座にグラフに反映（リアルタイムプレビュー）

#### 適用ロジック

- 「適用する」ボタンで `saveSavingsAllocation()` をコール
- fromYear に対応する entries エントリを追加/更新
- 金利は `interestRates` マップに保存（全期間共通）
- モーダルを閉じる

---

## 9. PJ詳細画面

```
┌──────────────────────────┐
│ < 使いみち              │ 戻る
│ 老後資金     現在の積立   │
│ 2050年定年   ¥2,500,000  │ 要注意
├──────────────────────────┤
│ ✦ AIインサイト           │
│ このペースでは2058年達成   │
│ 見込み（8年遅れ）...      │
├──────────────────────────┤
│  折れ線グラフ            │
│  - 試算ライン（緑）       │
│  - 目標★（金/灰）         │
│  - 出来事マーカー         │
│  - 軸切替: 生涯/5年/1年   │
├──────────────────────────┤
│ ←★2030 ★2034 ★2044→     │ 夢年表（横スクロール）
│   PJ色のドットで表示      │ グラフと連動
├──────────────────────────┤
│ 計画 / 実績 タブ          │
│  各時系列に出来事リスト    │
│  タップで編集可能         │
├──────────────────────────┤
│ [⚙ 積立を調整する]      │ 下部固定ボタン
└──────────────────────────┘
```

### グラフと夢年表の連動

- グラフ上の★/出来事をタップ → 下の夢年表で該当★がハイライト
- 夢年表の★をタップ → グラフ上の該当点が光る + ツールチップ表示
- ハイライトは3秒で自動解除

### 積立調整モーダル（下からスライドイン）

```
┌──────────────────────────┐
│  グラフ（薄く表示、       │
│  リアルタイム更新）       │
├──────────────────────────┤
│ 積立を調整         [×]   │
│                          │
│ 月の積立額                │
│ [−]  ¥30,000  [+]        │ ±ボタン
│                          │ タップで¥1,000刻み
│                          │ 長押しで¥10,000刻み
│                          │ 数字タップで直接入力
│                          │
│ 変更ポイント              │
│ ▶ 2025年〜 ¥30,000/月    │
│ ▶ 2030年〜 ¥50,000/月    │
│ [+ 変更ポイントを追加]   │
│                          │
│ [    適用する    ]       │
└──────────────────────────┘
```

操作中もグラフがリアルタイムで動く。

---

## 10. 夢年表画面

```
┌──────────────────────────┐
│ < 戻る   夢年表    [+]   │
├──────────────────────────┤
│                          │
│ ▼ 2020年代               │
│   2025年（今）           │
│   2027年                 │
│   ★ ハワイ旅行           │
│   あなた39歳             │
│   配偶者37歳             │
│   太郎2歳                │
│                          │
│   2030年                 │
│   ★ 1台目の車買い替え     │
│   あなた42歳             │
│   ...                    │
│                          │
│ ▼ 2030年代               │
│   2034年                 │
│   ★ 子の大学入学          │
│   ...                    │
│                          │
│ ▶ 2040年代  3つの夢       │ 折りたたみ
│ ▶ 2050年代  2つの夢       │
└──────────────────────────┘
```

### ★追加モーダル（+ボタンで開く）

```
新しい夢を立てる

いつ？
[ 2030年 ▼ ]   今開いてる年がデフォ

何を？
[ 車買い替え           ]

どのPJ？
[ 車PJ ▼ ]
└ + 新しいPJを作る

[ 保存 ]
```

PJ未紐付けは不可（必須）。新規PJはインラインで作成可能。

---

## 11. シナリオタブ

### 無料ユーザー（シナリオ1つ）

```
┌──────────────────────────┐
│ シナリオ                 │
├──────────────────────────┤
│ 現在のシナリオ            │
│ ┌────────────────┐       │
│ │ ● プランA：メイン │       │
│ │   選択中          │       │
│ │ 総資産 ¥825万    │       │
│ │ 夢 12個          │       │
│ └────────────────┘       │
├──────────────────────────┤
│ もう一つ試してみる？      │
│ ┌────────────────┐       │
│ │  ＋              │       │
│ │ もしもプランを    │       │
│ │ 追加する          │       │
│ │ 🔒 プレミアム機能 │       │
│ └────────────────┘       │
└──────────────────────────┘
```

### プレミアムユーザー（複数シナリオ）

```
┌──────────────────────────┐
│ シナリオ                 │
├──────────────────────────┤
│ ● プランA：メイン        │ 選択中
│   総資産 ¥825万           │
│   夢12個                  │
│                          │
│ ○ プランB：FIRE想定       │
│   総資産 ¥820万           │
│   夢10個                  │
│                          │
│ ○ プランC：ハワイ移住      │
│   総資産 ¥830万           │
│   夢14個                  │
│                          │
│ [+ シナリオを追加]        │
└──────────────────────────┘
```

### シナリオ切替時のUX

シナリオを選択した瞬間：

1. 全画面にロード中オーバーレイを表示
   ```
   ┌──────────────────────────┐
   │                          │
   │     ✦ 試算を読み込み中    │
   │                          │
   │     [ローディングアニメ]   │
   │                          │
   └──────────────────────────┘
   ```
2. 選択したシナリオの試算データを再読み込み
3. **ホーム画面に自動遷移**（シナリオタブには戻らない）
4. ホーム画面で新しいシナリオの内容が表示される

### シナリオ追加フロー

「+シナリオを追加」を押すと：

```
新しいシナリオを作る

┌───────────────────────────┐
│  📋 既存をコピーして編集   │
│  メインプランをベースに    │
│  少しだけ変えたい時に      │
└───────────────────────────┘

┌───────────────────────────┐
│  ✨ ゼロから作る           │
│  まったく違う人生プランを   │
│  最初からセットアップ      │
└───────────────────────────┘
```

#### コピーフロー
1. 既存シナリオを選択
2. 新シナリオ名入力（「FIREプラン」など）
3. すべてのデータがコピーされた状態で開く
4. 必要箇所だけ編集

#### ゼロからフロー
1. 新シナリオ名入力
2. オンボーディングフロー全体を再実行
3. 完成後、新シナリオに自動切替

### 命名規則

- システムラベル：プランA / プランB / プランC...（自動）
- ユーザー名：自由入力（「メイン」「FIRE想定」など）
- 表示形式：「プランA：メイン」「プランB：FIRE想定」

すべてのプランは対等（メインだけの特別扱いなし）。

---

## 12. オンボーディング

### 全体フロー

```
1. ウェルカムスライド（4枚）
2. 興味のカード選択
3. カテゴリごとの質問
4. プール金登録
5. 家族構成登録
6. 積立ルール
7. 給料日の設定 ← 重要
8. AI試算
9. 完成 → 使いみち画面に着地
```

最初に**夢の話から始める**ことで、ワクワク感を引き出す。事務的なデータ入力は途中で挟む。

### 進捗矢印（全画面共通・上部固定）

```
夢 ▶ 興味 ▶ 質問 ▶ 資産 ▶ 家族 ▶ 積立 ▶ 給料日 ▶ 試算
              ━━━ 現在地 ━━━
```

### Step 1: ウェルカムスライド（4枚）

| スライド | メッセージ |
|---|---|
| 1 | これは家計簿ではありません（過去の記録ではない） |
| 2 | 夢を、現実的に試算します |
| 3 | いつでも、いくらでも、変えられます |
| 4 | さあ、はじめましょう [始める] |

### Step 2: 興味のカード選択

3グループに分けて9枚のカード + 自由追加。

```
━━ 暮らしの基盤 ━━
[👴 老後] [🎓 教育] [🏡 家]

━━ ライフイベント ━━
[💍 結婚] [👶 出産] [🏥 介護]

━━ 暮らしを楽しむ ━━
[🚗 車] [✈️ 旅行] [🐾 ペット]

━━ その他 ━━
[+ 自由追加]
```

- カードタップで選択/解除（チェックマーク表示）
- 「次へ」ボタンは1つ以上選択で活性化
- 「自由追加」では、カテゴリ名と質問パターンをユーザーが定義可能

### Step 3: カテゴリごとの質問

選んだ順に1カテゴリ1画面ずつ。

**例：車**
```
🚗 車について教えてください

次の買い替えはいつ頃？
[ 2030年 ▼ ]

どんな車に乗りたい？
○ 軽自動車（〜200万）
● コンパクトカー（200〜350万）
○ SUV（350〜500万）
○ 高級車・輸入車（500万〜）

何年ごとに買い替える？
○ 5年  ● 7年  ○ 10年

(2／6カテゴリ)

[ 後で答える ]    [ 次へ ]
```

「後で答える」を選んだ夢は、AIが平均値で仮置きで試算する。

### Step 4: プール金登録

橋渡しメッセージから始める。

```
夢の輪郭ができました 🎉

次は、今の状況を教えてください
あなたの夢を、現実の数字と
照らし合わせて試算します

[ 続ける ]
```

その後、口座と残高を入力。

### Step 5: 家族構成登録

- あなた（必須）
- 配偶者（任意）
- 子ども（任意・複数可）
- ペット（任意・複数可）
- その他（任意・祖父母など）

各メンバーに名前と生年（誕生年）。

### Step 6: 積立ルール

各PJに対して月いくら積み立てるか設定。

### Step 7: 給料日の設定

```
給料日を教えてください

毎月の入金日を登録すると、
✦ AIが自動で試算を更新します

給料日：[ 25日 ▼ ]
入金額：[ ¥450,000     ]

※ 後で変更できます

[ 次へ ]
```

給料日に自動で試算が更新される（後述「通知設計」参照）。

### Step 8: AI試算（ローディング演出）

```
✦ AIが試算しています...

✓ 車PJを試算しました ¥250万
✓ 教育PJを試算しました ¥800万
✓ 旅行PJを試算しました ¥80万
✓ 老後PJを試算しました ¥3,000万
○ 全体最適化中...
```

### Step 9: 完成 → 使いみち画面に着地

専用の完成画面は作らない。**そのまま使いみち画面に遷移し、AIインサイトが結果を語る**。

---

## 13. AI仕様

### AIの役割と技術選択

| 機能 | 実装場所 | LLM使用 |
|---|---|---|
| 数値計算（達成年予測） | ローカル | ❌ |
| カテゴリ別相場参照 | ローカルテーブル | ❌ |
| 状況判定（安心/希望切替） | ローカル | ❌ |
| **AIインサイトの文体整形** | **LLM** | ✅ |
| **夢年表の自動生成** | **LLM** | ✅ |
| 自由カテゴリの質問生成 | LLM | ✅ |

**設計原則：**
- 数字は絶対にローカル計算（LLMに嘘をつかせない）
- 文章はLLMで自然化（人格は持たせない）
- LLMは「文体を整えるシェフ」、数字は計算器

### AIインサイトの表現

**統一表記：「AIインサイト」**
**シグネチャ：✦ マーク**

| 場面 | 表記 |
|---|---|
| ホームのAI助言 | `✦ AIインサイト` |
| PJ詳細のAI助言 | `✦ AIインサイト` |
| オンボのAI試算演出 | `✦ AIが試算します` |
| 夢追加時のAI試算ボタン | `[ ✦ AIに試算してもらう ]` |
| シナリオ作成時のAI提案 | `✦ AIが必要な変更を提案します` |
| ローディング中 | `✦ AIが試算しています...` |

**キャラクター化はしない。** 名前を持たせず、機能として存在させる。

### 試算の相場データ

カテゴリ別に相場を持つ：

| カテゴリ | 試算ロジック |
|---|---|
| 車 | クラス別相場（軽/コンパクト/SUV/高級） |
| 旅行 | 行き先×人数×日数 |
| 教育 | 進路別（公立/私立、文系/理系、医歯薬） |
| 家 | エリア別坪単価、リフォーム箇所別相場 |
| 老後 | 年金収入 × 余命で必要額を逆算 |

### AIインサイトの生成ロジック例

**ステップ1：ローカルで状況判定**
```
主要PJ判定 = 老後・教育・もしも・住宅・介護のうち登録されているもの
1つでも未達成 → 安心系メッセージ
全部達成 → 希望系メッセージ
```

**ステップ2：テンプレ選択**
```
テンプレ "安心_老後遅れ":
  「{PJ名}の達成が{年数}年遅れ気味です。月+{金額}で計画通りになります」
```

**ステップ3：LLMで自然化**
```
LLMへの指示：
以下の情報を、ツカイドキAIインサイトとして3〜5文の自然な日本語にしてください。
温かいトーン、押し付けない、見守る感じ。
```

---

## 14. ゲーミフィケーション設計

### 基本方針

ツカイドキは**採点しない**。健全度・達成率%・スコアは持たない。

その代わり、4つの感情を画面ごとに分散配置する：

| 感情 | 配置場所 | 表現方法 |
|---|---|---|
| **安心** | ホーム画面 | 「あなたの備え」のステータス＋AIインサイト |
| **自信** | ホーム画面 | 「8PJ・12夢・4口座 管理中」の事実表示 |
| **希望** | ホーム画面（条件付き） | AIインサイトの希望系メッセージ |
| **冒険心** | シナリオタブ | 「もしも」を試せる場所 |

### 「あなたの備え」の判定ロジック

ホーム画面のAIインサイト切替の判定基準。

**主要PJ（生活防衛軸）：**
- 老後資金
- 教育資金
- もしもの備え（生活防衛費）
- 住宅資金（登録されている場合のみ）
- 介護資金（登録されている場合のみ）

**生活防衛費のデフォルト値：**
- 月の生活費 × 6ヶ月分（一般的な目安）
- ユーザーは設定画面で変更可能

**判定ロジック：**
```
主要PJのうち、現状ペースで届かないものが1つでもあれば
→ 安心系メッセージ（不足箇所を提示＋解決策）

すべて届く見込みであれば
→ 希望系メッセージ（余力で何ができるか提示）
```

### 「希望」の事前提示ロジック

希望系メッセージを出す時のAI提案：

- 余力（プール金合計 - ポートフォリオ合計）が一定以上ある
- ユーザーが立てていないカテゴリを優先的に提案
- 同じ提案は半年は繰り返さない

例：
- 「¥350万のSUVが買えます」
- 「家族でハワイ旅行に行けます」
- 「月10万の海外移住、視野に入ります」

### ステータス表現

PJの状態を示す表現：
- **✓** = 届く（緑、シンプル）
- **△** = 工夫の余地あり（オレンジ、テキストで具体的に）
- ✗ は使わない（誰にも「失敗」を突きつけない）

例：
```
老後資金       ✓ 届く
教育資金       ✓ 届く  
もしもの備え   △ あと¥150万
```

---

## 15. 通知設計

### 給料日の自動更新

オンボで設定した給料日（例：毎月25日）に：

1. その日にバックグラウンドで自動的にAI試算を再実行
2. AIインサイトを更新
3. 朝、プッシュ通知：
   ```
   ✦ 今月の試算を更新しました
   タップで確認
   ```

### 通知の最小主義

**送らない通知：**
- 毎日のリマインダー
- 「夢を立てましょう」などの煽り通知
- ランキング・比較系
- 「他の人は○○しています」

**送る通知：**
- 給料日の自動更新通知（月1回）
- 重要な変化があった時（任意で許可）
- マイルストーン到達時（夢の達成）

### 通知設定画面

ユーザーが通知頻度をコントロールできる：
- 給料日通知：ON/OFF
- 月次サマリー：ON/OFF
- マイルストーン通知：ON/OFF

---

## 16. マネタイズ

### プラン

| プラン | 価格 | 機能 |
|---|---|---|
| 無料 | ¥0 | シナリオ1つ、全機能利用可 |
| プレミアム | 月額 ¥◯◯◯ / 年額 ¥◯◯◯ | シナリオ無制限 |

### 課金トリガー

シナリオタブで「+追加シナリオ」を押した時のみ。
他の場所では課金提案しない（押し売り感を避ける）。

### 課金画面の文言

```
✨ プレミアム機能

もう一つの人生プランを
作りませんか？

こんなことができます
✓ FIREプランの試算
✓ 転職時のプラン
✓ 移住シミュレート
✓ 出産後のプラン
✓ 比較し放題

月額 ¥◯◯◯
年額 ¥◯◯◯（2ヶ月お得）

[ プレミアムにする ]
[ 今はやめておく ]
```

---

## 17. デザイン原則

### カラー

| 用途 | カラーコード |
|---|---|
| メインカラー（紺） | #0C447C |
| アクセント緑 | #1D9E75 |
| アクセントオレンジ | #EF9F27 |
| アクセント赤 | #E24B4A |
| アクセント紫 | #534AB7 |
| アクセントグレー | #888780 |
| 背景 | #f5f4ee |
| カード背景 | #ffffff |

### トーン&マナー

- フラット、角丸、最小限の装飾
- 0.5px borderの繊細な区切り
- ダークモード対応
- スマホ縦持ち基本（iPhone/Android）
- iOS Safari PWA対応

### 体験原則

1. **操作したら未来が動く**：ボタン、スライダー、入力すべてリアルタイムでグラフが反応
2. **AIは見守る存在**：押し付けず、ユーザーの自由意志を尊重
3. **入力は最小限**：4種類のみ。「振替」「配分」など複雑な概念は廃止
4. **夢が主役、お金は脇役**：オンボでも普段使いでも「夢の話」から始める
5. **押し売りしない**：通知・課金提案も控えめに
6. **思い出を持たない**：過去の振り返り機能は持たず、現在と未来の関係だけを映す
7. **採点しない**：健全度・達成率%は持たない。✓と△の2段階のみ

### 文言の方針

- 「計画通り」→ 言わない
- 「目標未達」→ 「あと月¥3,000で届きます」と解決策提示
- 「節約しましょう」→ 言わない
- 「使い時です」→ 後押しする
- 「失敗」「ダメ」「足りない」→ 「工夫の余地」「あと○○で安心」

---

## 18. Claude Codeへの指示

### データモデル

**Zustand + AsyncStorage（キー：`tsukaidoki-store`）**

```typescript
// シナリオ
scenarios: ScenarioMeta[]           // { id, systemLabel, userLabel }
activeScenarioId: string
scenariosData: Record<string, ScenarioData>

// アクティブシナリオのデータ（scenariosData[activeScenarioId] に保存）
balances: Record<projectId, number>
userEvents: Record<projectId, UserEvent[]>
dreams: Dream[]
dreamOrder: string[]
savingsAllocation: SavingsAllocation
  // entries: { fromYear, monthlyAmounts }[] （fromYear順にソート）
  // interestRates: Record<projectId, number>

// 全シナリオ共通
familyMembers: FamilyMember[]       // { id, name, role, birthYear }
onboardingDone: boolean
aiInsights: Record<string, string>
isPremium: boolean
paydayDay: number
paydayAmount: number
```

**主要アクション：**
- `updateBalance(projectId, prevAmount, newAmount, note)` — UserEvent を作成して残高更新
- `addTransfer(fromId, fromPrev, toId, toPrev, amount, note)` — 口座間振替（双方に UserEvent）
- `switchScenario(id)` — 現在をスナップショット → 対象を読み込み → ホームに遷移
- `addScenario(userLabel, copyFromId?)` — コピーまたは新規作成
- `saveSavingsAllocation(allocation)` — entries + interestRates を保存

### 定数データ（`constants/data.ts`）

**POOL_ITEMS**（5口座）：

| id | 名称 | デフォルト残高 | 色 |
|---|---|---|---|
| pool-shoken | 証券口座 | ¥3,000,000 | — |
| pool-teiki | 定期預金 | ¥2,500,000 | — |
| pool-nisa | 積立NISA | ¥1,000,000 | — |
| pool-main | メイン銀行 | ¥800,000 | — |
| pool-sub | サブ銀行 | ¥450,000 | — |

**PF_ITEMS**（5項目）：

| id | 名称 | デフォルト残高 | ステータス |
|---|---|---|---|
| edu | 教育資金 | ¥3,000,000 | ok |
| ret | 老後資金 | ¥2,500,000 | warn |
| car | 車資金 | ¥1,000,000 | ok |
| trip | 旅行資金 | ¥500,000 | ok |
| — | 余剰資金 | ¥1,150,000 | ok（projectId なし） |

### 実装済み機能（2026年4月時点）

| 機能 | 状態 | 備考 |
|---|---|---|
| 5タブナビゲーション | ✅ 実装済み | シナリオ・プール金・ホーム・使いみち・設定 |
| シナリオ切替・追加・削除・リネーム | ✅ 実装済み | ロードオーバーレイ + ホーム遷移 |
| ホーム画面（整合バナー・円グラフ・AIインサイト） | ✅ 実装済み | |
| プール金タブ（積み上げ面積グラフ・残高インライン編集） | ✅ 実装済み | BalanceSheet モーダルは廃止 |
| プール金 積立・複利モーダル（全画面・折れ線グラフ・from/to・横並び） | ✅ 実装済み | |
| 使いみち（配分バー・PJカード・並び替え） | ✅ 実装済み | |
| 使いみち ヘッダーに総資産表示 | ✅ 実装済み | |
| 使いみち 積立金調整パネル（残高インライン編集・月積立ステッパー・AI提案） | ✅ 実装済み | |
| PJ詳細（折れ線グラフ・出来事タイムライン・積立モーダル） | ✅ 実装済み | |
| 夢年表（decade別・追加モーダル・家族年齢表示） | ✅ 実装済み | |
| 口座振替（/transfer） | ✅ 実装済み | |
| オンボーディング（ウェルカム〜完了） | ✅ 実装済み | |
| プレミアム課金UI | ✅ 実装済み（スタブ） | 実際の課金処理は未実装 |
| AI試算（LLM連携） | ⬜ 未実装 | ローカルロジックのみ |
| 給料日の自動更新 | ⬜ 未実装 | |
| 銀行口座自動連携 | ⬜ 未実装 | |
| プッシュ通知 | ⬜ 未実装 | |

### 実装優先順位（次のフェーズ）

1. **AI機能（LLM連携）** — AIインサイトの文体整形・給料日自動更新
2. **オンボーディング改善** — 興味カード・給料日設定の動線強化
3. **プレミアム課金処理** — RevenueCat or 類似ライブラリ
4. **プッシュ通知** — 給料日通知・マイルストーン到達
5. **銀行口座自動連携** — Plaid または類似 API

### 実装時の注意事項

- **すべてのユーザーデータはシナリオごとに独立** — `switchScenario()` で完全スワップ
- **家族構成・給料日設定は全シナリオ共通**
- **残高変更は必ず `updateBalance()` 経由** — 履歴（UserEvent）が記録される
- **インライン編集の確定は `onBlur` で行う** — `onSubmitEditing` も同様にバインド
- **グラフのプレビュー更新はリアルタイム** — `useMemo` で draft 変更を即反映
- **コメントは書かない** — 変数名・関数名で意図を表現する

---

## 補足：開発時の心構え

ツカイドキは「**ユーザーの自由意志を尊重するアプリ**」です。
迷った時は以下のチェックリストで判断する：

- [ ] これは「採点」になっていないか？
- [ ] これは「押し付け」になっていないか？
- [ ] これは「過去の振り返り」になっていないか？
- [ ] これは「カーナビ的な提案」になっているか？
- [ ] これは「ユーザーが決められる」設計か？

すべて Yes（負の項目はNo）であれば、ツカイドキらしい機能と言える。

---

以上が実装指示書（完成版）です。
