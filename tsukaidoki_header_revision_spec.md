# ツカイドキ 実装修正指示書（ヘッダー改修）

最終更新：2026年4月

---

## 概要

プール金画面・使いみち画面の**ヘッダー部分の差額表示**を改修する。
既存の実装に対する**差分指示書**であり、他の仕様は実装指示書 v2 を参照すること。

---

## 1. 改修の目的

### 課題（現状）

1. ヘッダーの金額表示が大きすぎて画面上部を占有しすぎている
2. 差額表示が小さく、肝心な情報なのに目立たない
3. 自分側と相手側の名称・金額がバラバラに整列して**ガタガタした印象**を与えている
4. 差額の方向性（自分が多いのか少ないのか）が直感的に分からない
5. 7桁の金額表示（¥9,000,000など）が視覚的に重い

### 改修の方針

1. **金額を「万円」表示に統一**（千円以下は切り捨て）
2. **ヘッダーを2行構成に簡素化**：自分側の名称＋金額を1行目、差額を括弧書きで2行目
3. **プール金画面・使いみち画面のヘッダーをこの構造に統一**
4. **整合時は表示が変わる**ことで達成感を演出

---

## 2. 表記ルール

### 万円表示への統一

すべての**資産金額**は万円表示とする。千円以下は**切り捨て**。

| 種別 | 表記例 | 補足 |
|---|---|---|
| ヘッダーの自分側金額 | 900万円 | 大きく |
| ヘッダーの差額 | −20万円 / +20万円 | 括弧書き |
| 各PJ・口座の金額 | 300万円 | リスト内 |
| 円グラフ中央の金額 | ¥925万 | （既存通り） |
| 月額の積立額 | ¥40,000 | **円表示のまま**（万円未満が多いため） |
| AIインサイト内の月額 | 月+¥15,000 | 円表示 |

**端数処理：**
- ¥3,047,500 → **304万円**（切り捨て）
- ¥3,000,000 → **300万円**

### 数字と単位のスタイル

数字部分を主役、「万円」を控えめにする：
- 数字：通常サイズ・通常色
- 「万円」：数字より小さいサイズ・グレー色

例：「**900**万円」（数字26pt太字、単位14ptグレー）

### 差額の符号

- 自分側が**少ない**：`−20万円`（マイナス記号）
- 自分側が**多い**：`+20万円`（プラス記号）
- 整合：`プール金と一致 ✓` または `使いみちと一致 ✓`

---

## 3. ヘッダーレイアウト（決定版）

### 基本構造

ヘッダーは**2行構成**：

```
┌──────────────────────────────────┐
│ 使いみち  900万円                  │ ← 1行目：名称＋金額（大きく）
│ (プール金より −20万円)             │ ← 2行目：差額を括弧で（控えめに、オレンジ）
└──────────────────────────────────┘
```

### 視覚仕様

#### 1行目：自分側の名称＋金額
- **名称**：22pt 太字、通常テキスト色（黒）
- **金額（数字）**：26pt 通常ウェイト、通常テキスト色（黒）
- **「万円」単位**：14pt 通常ウェイト、グレー色
- 名称と金額の間：12px のgap
- 配置：**左寄せ**

#### 2行目：差額の括弧表示
- **テキスト全体**：13pt
- **「(プール金より 」「)」部分**：通常ウェイト、オレンジ色
- **金額部分（−20万円）**：通常ウェイト→**少し太字**、オレンジ色
- 配置：**1行目の名称の下から開始**（インデントなし）
- 1行目との間隔：4px

### 状態別の表示パターン

#### パターンA：使いみち画面 / 差額あり（自分側が少ない）

```
使いみち  900万円
(プール金より −20万円)
```

#### パターンB：使いみち画面 / 整合

```
使いみち  900万円
(プール金と一致 ✓)
```

- 括弧内の色を**緑（#27500A）**に変更
- 「✓」マークを末尾に

#### パターンC：プール金画面 / 差額あり（自分側が多い）

```
プール金  920万円
(使いみちより +20万円)
```

#### パターンD：プール金画面 / 差額あり（自分側が少ない）

```
プール金  900万円
(使いみちより −20万円)
```

#### パターンE：プール金画面 / 整合

```
プール金  920万円
(使いみちと一致 ✓)
```

---

## 4. インライン編集中のリアルタイム反映

既存仕様（v2）と同じく、編集中は2行目の差額がリアルタイムで動く。

### 編集中の挙動

**例：使いみち画面で老後資金の金額を編集**

```
[編集前]
使いみち  900万円
(プール金より −20万円)

↓ 老後資金を 300万円 → 320万円 に編集中

[編集中（リアルタイム）]
使いみち  920万円        ← 数字がヌルッと変わる
(プール金より +0万円)    ← 差額がリアルタイム更新

↓ 整合した瞬間

[整合]
使いみち  920万円
(プール金と一致 ✓)       ← オレンジ→緑にフェード
```

### アニメーション仕様

- 数字の変化：`transition: 0.15s ease`
- 整合時の色変化：`transition: color 0.3s ease`
- 整合の達成感を演出するが、過剰にならない範囲で

---

## 5. CSSスペック（参考）

### ヘッダーコンテナ

```css
.hdr {
  padding: max(env(safe-area-inset-top), 16px) 20px 18px;
  flex-shrink: 0;
}
```

### 1行目：自分側

```css
.row-main {
  display: flex;
  align-items: baseline;
  gap: 12px;
  margin-bottom: 4px;
}
.row-main .name {
  font-size: 22px;
  font-weight: 600;
  color: var(--text-primary);
  letter-spacing: -0.3px;
}
.row-main .amount {
  font-size: 26px;
  font-weight: 500;
  color: var(--text-primary);
  letter-spacing: -0.5px;
}
.row-main .unit {
  font-size: 14px;
  font-weight: 500;
  color: var(--text-secondary);
  margin-left: 2px;
}
```

### 2行目：差額の括弧表示

```css
.diff-paren {
  font-size: 13px;
  color: var(--warn);    /* オレンジ #9c5800 */
  font-weight: 500;
  transition: color 0.3s ease;
}
.diff-paren strong {
  font-weight: 600;
}
.diff-paren.ok {
  color: var(--ok);      /* 緑 #27500A */
}
```

### HTML構造例

**差額あり：**
```html
<div class="hdr">
  <div class="row-main">
    <div class="name">使いみち</div>
    <div>
      <span class="amount">900</span>
      <span class="unit">万円</span>
    </div>
  </div>
  <div class="diff-paren">
    (プール金より <strong>−20万円</strong>)
  </div>
</div>
```

**整合時：**
```html
<div class="hdr">
  <div class="row-main">
    <div class="name">使いみち</div>
    <div>
      <span class="amount">920</span>
      <span class="unit">万円</span>
    </div>
  </div>
  <div class="diff-paren ok">
    (プール金と一致 ✓)
  </div>
</div>
```

---

## 6. リスト内の金額表示も「万円」に統一

ヘッダーだけでなく、**PJカード・口座カードの金額表示も万円に統一する**。

### Before（現状）
```
老後資金              ¥3,000,000 ✏
教育資金              ¥4,000,000 ✏
```

### After（修正後）
```
老後資金              300万円 ✏
教育資金              400万円 ✏
```

### スタイル

```css
.item-amt {
  font-size: 15px;
  font-weight: 500;
  color: var(--accent-blue);
}
.item-amt .unit {
  font-size: 11px;
  margin-left: 1px;
  color: var(--text-secondary);
  font-weight: 400;
}
.item-amt .pen {
  font-size: 10px;
  color: var(--text-tertiary);
  margin-left: 4px;
}
```

```html
<div class="item-amt">300<span class="unit">万円</span><span class="pen">✏</span></div>
```

### 編集モード時

タップでインライン編集に入る時も、**入力フィールドも万円単位**にする：

```
[通常]    300万円 ✏
↓ タップ
[編集中]  [ 300 ] 万円  ← 数字部分だけが入力欄、「万円」は固定
```

数字キーパッドは万円単位での入力（300 と打てば 3,000,000円）。
**入力ハードルが大幅に下がる**。

---

## 7. 既存実装からの変更点まとめ

| 項目 | Before | After |
|---|---|---|
| ヘッダーの金額単位 | 円表示（¥9,000,000） | 万円表示（900万円） |
| ヘッダー構造 | 4要素（自分名称・自分金額・相手名称・相手金額・差額が散在） | 2行（自分側／差額の括弧表示） |
| ガタガタ感 | あり | なし |
| 差額の方向性 | 不明 | +/− 符号で明示 |
| プール金の絶対額 | ヘッダーに表示 | **削除**（プール金画面で確認可能なので不要） |
| リスト内金額 | 円表示 | 万円表示 |
| インライン編集の入力 | 円単位 | 万円単位 |

---

## 8. 確認チェックリスト

実装後、以下を確認すること：

- [ ] ヘッダーが**2行**で表示されている
- [ ] 1行目の「使いみち / プール金」と金額が左寄せで揃っている
- [ ] 2行目の差額がオレンジ色の括弧書きで表示されている
- [ ] 千円以下が**切り捨て**で表示されている
- [ ] 自分が少ない時：`(◯◯より −◯万円)`
- [ ] 自分が多い時：`(◯◯より +◯万円)`
- [ ] 整合時：`(◯◯と一致 ✓)` で**緑色**
- [ ] インライン編集中、2行目の差額が**リアルタイム更新**される
- [ ] 整合した瞬間、オレンジ→緑に**0.3秒でフェード**
- [ ] PJカード・口座カードの金額も**万円表示**になっている
- [ ] 月額（積立額など）は**円表示のまま**
- [ ] AIインサイトのレイアウトに変化なし

---

以上が修正指示書です。
