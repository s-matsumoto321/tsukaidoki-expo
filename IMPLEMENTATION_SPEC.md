# ツカイドキ 構造刷新 実装指示書

> 対象：Claude Code
> スコープ：フロントエンド実装（画面・UI・状態管理）。データはモックで可。
> 前提：既存のツカイドキアプリのコードベース上で改修する。配色・フォント・カードコンポーネントの基本デザインは既存を踏襲。

---

## 0. 全体方針

### 0.1 設計思想

ツカイドキのコア体験は「**シミュレーションした未来を、安心して、希望を見ること**」。
そのために必要な3要素は維持する：

1. **シミュレーション**（積立と支出による未来の推移）
2. **実績との一致確認**（プール金の口座残高更新で代替）
3. **整合性チェック**（差分があったときだけ通知）

ただし、これらを **同じ画面で同居させない**。お金の流れに沿った3画面構成にする。

### 0.2 画面の流れ

```
① プール金 ─→ ② 使いみち ─→ ③ 使いみち詳細
   (起点)        (配分)         (支出入力)
```

- ①で「いくら持っていて、毎月いくら積み立てるか」を決める
- ②で「①の積立を目的別にどう分けるか」を決める
- ③で「いつ、何に、いくら使うか」を入れる
- ③の支出が積立を上回ると赤字 → ②に戻って調整

### 0.3 大きな構造変更（重要）

| 項目 | Before | After |
|---|---|---|
| ホームタブ | あり | **廃止** |
| タブ構成 | シナリオ / プール金 / ホーム / 使いみち / 設定（5タブ） | シナリオ / プール金 / 使いみち / 設定（**4タブ**） |
| 計画 vs 実績 | 使いみち詳細で二重入力 | **計画のみ。実績概念は廃止** |
| 整合性表示 | 常時バッジ（「✓整合」「使いみちより+30万」など） | **常時表示は廃止**（差分時のみ通知、本リリースでは未実装） |
| ランディング | ホーム | **使いみち** |

### 0.4 削除する画面・コンポーネント

- ホーム画面（`/home` ルート）
- 使いみち詳細画面の「計画 / 実績」タブと、「実績」記録モーダル（IMG_2191, IMG_2196 のUI）
- 「(プール金より +30万)」「(使いみちと一致✓)」などの整合性表示

---

## 1. ① プール金画面の改修

### 1.1 既存の維持部分

- 画面タイトル「プール金」と総資産（右上）
- 上部の青系の面グラフ（既存のまま）
- 口座カード一覧
- 「積立・複利を調整する」ボタンと、それを押した時のモーダル
- 各口座カードの左ボーダー色（青系のグラデーション）

### 1.2 変更点

#### 1.2.1 整合性表示の削除

- 総資産下にある **「(使いみちより +30万円)」表示を削除**
- 代わりに、総資産下に「**月+10万 積立中**」のテキストを追加（緑色、月の積立合計）

#### 1.2.2 各口座カードに月積立・複利の小さなメタ表示を追加

```
┌──────────────────────────────────┐
│ 証券口座            350万円 ✎ │
│ ─────────────────────────────── │
│ 月積立 5万円  複利 5.0%        │ ← 新規追加
└──────────────────────────────────┘
```

- 既存のカード本体（名前・残高・編集ペンシル）はそのまま
- 下に1行追加：左側に「月積立 X万円」、左から少し離して「複利 X.X%」
- フォントサイズ 11px、色は `--ink-mute`、数値部分のみ `--blue` で太字
- 上のメイン部分との区切りは破線 `1px dashed #ecedef` を `padding-top: 6px; border-top` で

### 1.3 ファイル変更箇所（推定）

- 該当画面コンポーネント（例: `src/screens/PoolScreen.tsx` など）
- 整合性差分表示のロジックがあれば削除
- 口座カードコンポーネント（例: `src/components/AccountCard.tsx`）にメタ行を追加

---

## 2. ② 使いみち画面の改修（最重要）

### 2.1 既存の維持部分

- 画面タイトル「使いみち」と総資産（右上）
- AIインサイトの薄緑カード
- 各使いみちカード（教育・老後・車・旅行）の基本構造とボーダー色
  - 教育: 紺 (`--blue-deep`)
  - 老後: 緑 (`--green`)
  - 車: 橙 (`--orange`)
  - 旅行: 紫 (`--purple`)
- 下部の「積立を調整する」ボタン

### 2.2 大きな変更点

#### 2.2.1 整合性表示の削除

- 総資産下の **「(プール金より −30万円)」表示を削除**

#### 2.2.2 画面上部に「使いみち推移グラフ」を新規追加

**配置**：AIインサイトの **上**（画面タイトル直下）

**仕様**：

- 4本の折れ線。各使いみちの **残高推移** を表示
  - 横軸: 年（'25 〜 '55、現在から退職まで）
  - 縦軸: 残高（0 〜 800万、自動スケーリング）
  - **0ライン（横軸）を太め（1.5px、`--ink-mute` 色）に描画**
- 4色（紺・緑・橙・紫）で各使いみちを表現
- 凡例は **不要**（下のカードが凡例代わり）
- 高さ 200px、`<svg viewBox="0 0 340 200">` で実装
- カード背景は白、`border-radius: 16px`、`padding: 14px 14px 10px`

**赤字の見せ方**（3層）：

1. **0を下回る区間の線を赤色 (`--red`)** に変える
2. 該当区間の **背景に薄い赤の網掛け** (`rgba(199, 69, 69, 0.08)`)
3. 線の最下点（赤字のピーク）に **赤い⚠マーカー**（半径4pxの円 + ⚠テキスト）
4. グラフ下に赤字フッター：
   ```
   ⚠ 2027年に旅行資金が赤字になります
   ```
   - 背景 `--red-bg`、テキスト `--red`、太字、`border-radius: 8px`
5. 赤字がない場合、フッターは表示しない（または「✓ すべて順調です」を緑で表示）

**横軸ラベル**: グラフ下に '25 / '30 / '35 / '40 / '45 / '50 / '55 を等間隔で配置

#### 2.2.3 各使いみちカードから「金額表記」を削除

> 月積立額は期間によって変わるため、固定で1つの金額を表示するのは不正確。

- カード内の `月積立 X万円` のような **積立額の固定表示は表示しない**
- 残す情報：
  - 使いみち名（教育資金 など）
  - ステータス（✓ 順調 / △ 調整の余地 / ⚠ 赤字 −XX万）
  - 右側の現在残高（既存のまま）

#### 2.2.4 赤字の使いみちカードの強調

赤字の使いみち（例: 旅行資金）：

- カード背景を **薄い赤グラデーション**：`linear-gradient(180deg, #ffffff 0%, #faf0ee 100%)`
- ステータス行：`⚠ 赤字 −30万` を赤太字で
- 右側の残高数値も赤色 (`--red`) に

### 2.3 「積立を調整する」モーダル（新規実装）

#### 2.3.1 モーダルの基本構造

「積立を調整する」ボタンを押すと、画面下から **半分（高さ 50%）** の位置までモーダルが立ち上がる。

- モーダル背景: `var(--bg)` (`#f5f0e6`)
- 上端に grabber（幅40px・高さ4pxの灰色バー）
- 角丸: 上端のみ `border-radius: 24px 24px 0 0`
- 影: `box-shadow: 0 -4px 24px rgba(31, 42, 46, 0.12)`
- 背景にバックドロップ（半透明黒）。ただし **画面上部のグラフは透けて見える状態にする**

**モーダル背後の画面**：

- AIインサイトと使いみちカードは `opacity: 0.4` 程度に薄くする
- 上部の推移グラフは **そのまま見える**（後述のリアルタイム連動のため）
- タブバーも薄くする

#### 2.3.2 モーダル内のレイアウト（上から順）

```
┌─ ══ ─────────────────────────┐  ← grabber
│ 積立を調整する        ✕       │  ← ヘッダー
│                                │
│ [生涯] [5年] [1年]             │  ← ピルタブ（既存スタイル）
│                                │
│ ┌──────────────────────────┐ │
│ │ 適用期間                   │ │
│ │ 2026 〜 2059  期間を選ぶ ›│ │  ← 折りたたまれた状態
│ └──────────────────────────┘ │
│                                │
│ ● 教育資金                    │
│   [−] 3万円/月 [+]            │
│                                │
│ ● 老後資金                    │
│   [−] 3万円/月 [+]            │
│                                │
│ ● 車資金                      │
│   [−] 2万円/月 [+]            │
│                                │
│ ● 旅行資金     ⚠ 赤字         │
│   [−] 2万円/月 [+]            │
└────────────────────────────────┘
```

#### 2.3.3 期間セクション（折りたたみ → 展開）の挙動

**折りたたみ状態**（初期）：

- 1行のみ表示：「適用期間」ラベル + 「2026 〜 2059」の値 + 「期間を選ぶ ›」リンク
- カード背景は白、`box-shadow` あり

**展開状態**（「期間を選ぶ ›」タップ後）：

- モーダル全体の高さが **52% → 78%** に拡大（アニメーション、300ms ease-out）
- 期間カード内に **俯瞰の積み上げ面グラフ** + **家族の年齢軸** + **範囲スライダー** が現れる
- ヘッダーが `背景: --green-bg` に変わり、リンクが「▼ 閉じる」になる

**展開状態の中身（詳細）：**

```
┌──────────────────────────────────┐
│ 適用期間（子供3〜5歳期）            │ ← 期間ラベルが文脈付きに
│ 2028 〜 2030      ▼ 閉じる        │
│ ───────────────────────────────  │
│ 配分の歴史と家族の年齢を見ながら   │
│ 期間を選ぶ                          │
│                                  │
│  10万 ┄┄┄┄┄┄┄┄┄┄ 上限 月10万     │
│  ┌─[積み上げ面グラフ]─┐           │ 高さ140px
│  │  ▓旅行▓                │       │
│  │  ▒車▒                   │       │ 緑の半透明帯
│  │  ▓老後▓                 │       │ で選択範囲
│  │  ▓教育▓                 │       │ ハイライト
│  │_______________________│       │
│   '26  '30  '34 ... '58           │
│                                  │
│  年    '26 '30 '34 ...            │
│  あなた  36  40  44 ...           │
│  妻    34  38  42 ...             │
│  子     1   5   9  ...           │ ← 「5」が緑ハイライト
│                                  │
│  ●━━━━━━━━━━━○━━━━━━━○━━━━●     │ ← 範囲スライダー
└──────────────────────────────────┘
```

**俯瞰グラフの仕様：**

- 4色の積み上げ面グラフ（下から: 教育・老後・車・旅行）
- 縦軸: 0〜10万円の月額。`--ink` 色の点線で **「上限 月10万」** を上端に表示
- 横軸: 期間を年単位で
- 選択範囲（From〜To）に **緑の半透明帯** (`rgba(90, 130, 102, 0.10)`) を描画
- 帯の左右両端に緑の点線 (`stroke-dasharray="2 2"`)
- データ構造の例：
  ```typescript
  type AllocationPeriod = {
    fromYear: number;
    toYear: number;
    edu: number;    // 月額（万円）
    retire: number;
    car: number;
    travel: number;
  };
  ```
- 期間ごとに段差ができる（積立配分の歴史が見える）

**家族の年齢軸：**

- 4行表示（`grid-template-columns: 50px 1fr` で左ラベル+右に等間隔の年齢）
  1. **年**（'26, '30, '34, ...）— 太字、`--ink`
  2. **あなた**（36, 40, 44, ...）— `--ink`
  3. **妻**（34, 38, 42, ...）— `--ink` ※「パートナー」の名前は設定で変えられる前提
  4. **子**（1, 5, 9, ...）— `--ink`
- 子供が1人の場合の表示。複数の場合は「子1」「子2」と分ける（将来対応）
- 選択範囲内に該当する年齢セルは：
  - 背景 `--green-bg`、文字 `--green` 太字
  - 例: 期間2028〜2030なら、子の「5」（2030年時点）がハイライト
- フォントサイズ 9.5px

**範囲スライダー：**

- 2つのつまみ（from / to）で範囲指定
- レール `#ecedef`、選択範囲は `--green` で塗り
- つまみ: 直径20px、緑、白い枠付き
- 年単位でスナップ
- スライダーを動かすと：
  - 上の積み上げグラフの **緑の選択範囲帯** がリアルタイムで動く
  - 年齢軸の **ハイライトされる年齢セル** がリアルタイムで切り替わる
  - 期間ラベルの「2028 〜 2030」の数値もリアルタイムで更新

#### 2.3.4 各使いみちの設定（+/-ステッパー）

各使いみちごとに1つのカード（白背景、`border-radius: 14px`）：

```
┌──────────────────────────────┐
│ ● 教育資金                    │
│ ┌────────────────────────┐  │
│ │ [−]   3万円/月    [+]  │  │
│ └────────────────────────┘  │
└──────────────────────────────┘
```

**仕様：**

- 左に色付きドット（10px円）+ 名前
- 赤字の使いみちには右に「⚠ 赤字」バッジ（`--red-bg` 背景、`--red` 文字）
- 赤字解消時は「✓ 赤字解消」バッジ（`--green-bg` 背景、`--green` 文字）
- ステッパー：背景 `--bg`、`border-radius: 10px`、`padding: 6px`
  - +/- ボタン: 32px x 32px、白背景、`box-shadow`
  - 中央に「3万円/月」（17px、太字）
  - +/- 押下中は該当ボタンを **緑背景 + 1.2sのpulseアニメーション** に
  - 値が変化した直後の数値は **緑色** に変える（`--green`、500ms後に通常色に戻す）

**ロジック：**

- 4つの使いみちの月積立合計が、**①で決めた限度額（月10万）を超えられない**
- 超えそうな操作は + ボタンを無効化（`disabled`、`opacity: 0.4`）
- 超えそうな時にAIインサイト的なヒントを画面どこかに表示してもよい（「車を減らしてから増やせます」など）

#### 2.3.5 リアルタイムプレビュー（核心の体験）

+/- ボタンを押した瞬間：

1. **モーダル背後のメイングラフ**（②画面上部）が動く
   - 旅行ラインなど、調整対象の使いみちラインが新しい値で再計算される
   - 旧ラインは **薄い点線で残る**（プレビュー中であることを示す）
   - 新ラインは **太い実線**
   - 赤字が解消した瞬間、⚠マーカー → ✓マーカー（緑円）に切り替え
   - 赤字フッターも「⚠ 2027年に...」→「✓ 赤字が解消されました」に
2. **画面上部の総資産横**に「**プレビュー中**」（緑、12px、太字）を表示
3. グラフ凡例の該当使いみちに **LIVEバッジ**（緑背景、白文字、点滅）

#### 2.3.6 モーダルの保存・キャンセル

- 「✕」ボタン: モーダルを閉じる。**変更は保存しない**（プレビューも消える）
- モーダル外をタップ（バックドロップ）: 同じくキャンセル
- モーダル下端に「保存する」ボタンを設置するか、自動保存（変更が即反映）かは要検討。
- **推奨：自動保存**（モーダル閉じれば確定）。子育てユーザーの操作回数を減らすため。

### 2.4 ファイル新規・変更箇所（推定）

新規：
- `src/components/UsageTrendChart.tsx`（4本ライン推移グラフ）
- `src/components/AdjustmentModal.tsx`（積立調整モーダル）
- `src/components/PeriodPicker.tsx`（期間選択 折りたたみセクション）
- `src/components/StackedAllocationChart.tsx`（俯瞰の積み上げ面グラフ）
- `src/components/AgeAxis.tsx`（家族の年齢軸）
- `src/components/RangeSlider.tsx`（範囲スライダー）
- `src/components/GoalAllocationStepper.tsx`（使いみちごとの+/-）

変更：
- `src/screens/UsageScreen.tsx`（②画面）

---

## 3. ③ 使いみち詳細画面の改修

### 3.1 既存の維持部分

- 戻るリンク「‹ 使いみち」
- タイトルブロック（教育資金 / 2044年 大学入学まで）
- 右上の「現在の積み立て」「目標 ¥500万」
- AIインサイト
- 折れ線グラフ（緑の試算ライン + 黄の夢★マーカー + 赤の支出マーカー）
- 「生涯 / 5年 / 1年」のピルタブ

### 3.2 大きな変更点

#### 3.2.1 「計画 / 実績」UIを完全削除

以下を削除：

- グラフ下の **「[積立] [支出]」 タブ切り替えボタン** → そのまま残す（積立年表/支出年表の切り替えとして機能）
- 「計画 / 実績」 の **2カラムタブ**
- 各イベントカードの「実績」側カード（`記録する` の点線ボタン）
- 画面下部の「**支出実績を記録**」ボタン → **「支出計画を編集」に変更**
- 月別の予定 vs 実績モーダル（IMG_2192, IMG_2196）も削除

> 既存実装は計画と実績を左右2カラムで並べる構造になっているが、**計画のみの1カラム**にする。

#### 3.2.2 タイトル部の表記変更

- 「目標 ¥500万 · **計画通り**」 から **「計画通り」を削除**
- 「目標 ¥500万」のみ表示

#### 3.2.3 グラフの表示変更

- 凡例から「実績」「計画」の文字を削除（凡例は「試算 / 夢★ / 支出」のみ）
- 緑の実績ライン と 青点線の計画ラインの **2本を1本に統合**：
  - **試算ライン1本のみ**（緑実線、`--green`、2.5px）
  - 計画通りの推移を1本で表現
- マーカー：
  - 黄丸（夢★イベント、`--yellow`）
  - 赤丸（支出イベント、`--red` または `--orange`）

#### 3.2.4 「積立 / 支出」タブの動作

グラフ下の「[📈 積立] [💰 支出]」のトグル：

- **積立タブ**選択時：年表に「積立計画」が並ぶ
  - 「2025 / 積立スタート / 月¥30,000配分中 / +¥30,000/月」のようなカード
  - その積立が始まる年代と金額の予定
- **支出タブ**選択時：年表に「支出計画」が並ぶ
  - 「2027 / 保育園費用 始動 / 支出が始まるが積立が上回る / −¥720,000/年」のようなカード
  - 各支出マーカー（赤円）に対応するイベント

各カードをタップすると、その項目の編集モーダルが立ち上がる。

#### 3.2.5 編集モーダル（積立・支出共通の構造）

**積立の編集モーダル**（IMG_2191相当）：

```
┌─ ══ ─────────────────────────┐
│ いくら積み立てる？               │
│ 計画 ・ 2026年〜                │
│                                │
│ 月の積立額                      │
│ [−]    3万円/月    [+]         │
│                                │
│                                │
│ [閉じる]      [保存する]        │
└────────────────────────────────┘
```

- ②モーダルとほぼ同じUI（+/-ステッパー）
- ただし、**ここでは「②で変更」の誘導**ではなく、編集も可能にする（②画面に戻ってもOK、ここで直接編集してもOK）
- 注意：ここで変更した内容は ②画面の積立配分にも反映される（同じデータソース）

**支出の編集モーダル**（IMG_2194相当）：

```
┌─ ══ ─────────────────────────┐
│ いつ、いくら使う予定？            │
│ 計画 ・ 保育園費用 始動            │
│                                │
│ 単位                            │
│ [年単位] [月単位]               │
│                                │
│ 金額                            │
│ [−]    72万円/年    [+]        │
│                                │
│                                │
│ [閉じる]      [保存する]        │
└────────────────────────────────┘
```

- 単位切り替え（年単位 / 月単位）：ピルタブ
- 金額の +/- ステッパー
- 単位を切り替えると数値も自動換算（72万/年 ⇄ 6万/月）

#### 3.2.6 「+ 支出を追加」のテンプレート入力

支出タブで「+支出を追加」をタップ → **教育費テンプレート選択**：

```
┌─ ══ ─────────────────────────┐
│ 支出を追加                       │
│                                │
│ ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐ │
│ │幼│ │小│ │中│ │高│ │大│ │習│ │
│ │稚│ │学│ │学│ │校│ │学│ │い│ │
│ │園│ │校│ │校│ │ │ │ │ │事│ │
│ └──┘ └──┘ └──┘ └──┘ └──┘ └──┘ │
└────────────────────────────────┘
```

各カテゴリをタップすると次のステップ：
- 幼稚園 → 公立 / 私立 を選ぶ → 年額が自動入力（公立 ¥72,000/年、私立 ¥158,000/年など、参考値）
- 小学校 → 公立 / 私立
- 中学校 → 公立 / 私立
- 高校 → 公立 / 私立
- 大学 → 国公立 / 私立文系 / 私立理系 / 私立医歯薬
- 習い事 → 月謝額を直接入力（自由）

**参考値テーブル**（モックで実装する値）：

| カテゴリ | 区分 | 年額目安 |
|---|---|---|
| 幼稚園 | 公立 | 72万円 |
| 幼稚園 | 私立 | 158万円 |
| 小学校 | 公立 | 31万円 |
| 小学校 | 私立 | 152万円 |
| 中学校 | 公立 | 49万円 |
| 中学校 | 私立 | 140万円 |
| 高校 | 公立 | 46万円 |
| 高校 | 私立 | 97万円 |
| 大学 | 国公立 | 67万円 |
| 大学 | 私立文系 | 110万円 |
| 大学 | 私立理系 | 150万円 |
| 大学 | 私立医歯薬 | 350万円 |

> 出典は文部科学省の各種調査の参考値を想定。実装時はモックでハードコード可。

選択後、年表に新しいイベントカードとして追加され、月別に按分された支出データが内部的に登録される。

### 3.3 ファイル変更箇所（推定）

- `src/screens/GoalDetailScreen.tsx`（③画面）
  - 計画/実績タブの完全削除
  - グラフのライン統合
  - イベント年表の単一カラム化
- 既存の「実績記録」関連ファイル → **削除**（または無効化）
- 新規モーダル：
  - `src/components/SavingEditModal.tsx`（積立編集）
  - `src/components/ExpenseEditModal.tsx`（支出編集）
  - `src/components/ExpenseTemplatePicker.tsx`（教育費テンプレート選択）

---

## 4. データ構造（モック）

実装時の参考のため、想定データ構造を示す。実装はTypeScriptの型として定義する。

```typescript
// プール金（口座）
type Account = {
  id: string;
  name: string;          // "証券口座"
  balance: number;        // 残高（円）
  monthlyContribution: number; // 月積立額（円）
  annualReturn: number;   // 複利（%、小数）
  colorTone: 'deep' | 'mid' | 'light' | 'pale' | 'very-pale';
};

// 使いみち
type Goal = {
  id: string;
  name: string;          // "教育資金"
  category: 'edu' | 'retire' | 'car' | 'travel';
  targetYear: number;    // 2044
  targetAmount: number;  // 5,000,000
  currentBalance: number;
  description?: string;  // "大学入学まで"
};

// 期間別の積立配分
type AllocationPeriod = {
  id: string;
  fromYear: number;
  toYear: number;
  allocations: {
    [goalId: string]: number; // 月額（円）
  };
};

// 支出計画
type ExpensePlan = {
  id: string;
  goalId: string;
  year: number;
  category: string;       // "幼稚園" "小学校" など
  subCategory?: string;   // "公立" "私立" など
  annualAmount: number;
  description?: string;
  startYear: number;
  endYear: number;       // 開始〜終了の期間
};

// 家族メンバー
type FamilyMember = {
  id: string;
  role: 'self' | 'partner' | 'child';
  name: string;
  birthYear: number;
};
```

---

## 5. 計算ロジック（フロントエンド側で実装）

### 5.1 使いみち推移グラフのデータ生成

```typescript
function calcGoalBalanceOverTime(
  goal: Goal,
  allocations: AllocationPeriod[],
  expenses: ExpensePlan[],
  fromYear: number,
  toYear: number
): { year: number; balance: number }[] {
  // 各年について、その時点での残高を計算
  // 1. 前年の残高
  // 2. + その年の積立（月額 × 12、該当期間の配分から取得）
  // 3. + 複利（口座の利率を加重平均）
  // 4. - その年の支出（expensesから集計）
  // 結果を配列で返す
}
```

### 5.2 赤字検出

```typescript
function detectDeficit(
  balanceOverTime: { year: number; balance: number }[]
): { hasDeficit: boolean; firstDeficitYear?: number; minBalance?: number } {
  // balanceが0未満になる最初の年を検出
}
```

### 5.3 限度額チェック（②モーダル）

```typescript
function getRemainingAllocation(
  totalMonthlyPool: number,  // ①で決めた合計（10万円）
  currentAllocations: { [goalId: string]: number }
): number {
  return totalMonthlyPool - Object.values(currentAllocations).reduce((a, b) => a + b, 0);
}

function canIncreaseAllocation(
  goalId: string,
  step: number,  // +1万円
  totalMonthlyPool: number,
  currentAllocations: { [goalId: string]: number }
): boolean {
  return getRemainingAllocation(totalMonthlyPool, currentAllocations) >= step;
}
```

---

## 6. 配色トークン

既存のCSS変数を維持。新規追加分のみ記載。

```css
:root {
  /* Existing */
  --bg: #f5f0e6;
  --card: #ffffff;
  --ink: #1a2226;
  --ink-soft: #4a5a60;
  --ink-mute: #8a9499;
  --green: #5a8266;
  --green-bg: #dde7d8;
  --blue: #4a7ba6;
  --blue-deep: #2c4a6b;
  --orange: #d97a4a;
  --purple: #8e7aa8;
  --yellow: #c89a4a;

  /* New additions */
  --red: #c74545;       /* 赤字表示 */
  --red-bg: #f8e6e3;    /* 赤字カード背景 */
  --red-zone: rgba(199, 69, 69, 0.08); /* グラフ上の赤字ゾーン */
}
```

---

## 7. 実装の優先順位

以下の順で実装を推奨：

1. **タブバーの変更**（ホーム削除、4タブ化）— 影響範囲が大きいので最初に
2. **③ 使いみち詳細画面の計画/実績削除** — 既存ファイルの削除作業
3. **② 使いみち画面の上部グラフ追加** — 新規コンポーネント
4. **② 積立調整モーダルの基本形**（折りたたみ期間 + 各使いみちの+/-）
5. **モーダル × メイングラフのリアルタイム連動**
6. **② 積立調整モーダルの期間展開機能**（俯瞰グラフ + 年齢軸 + 範囲スライダー）
7. **③ 教育費テンプレート選択モーダル**
8. **① プール金画面の整合性表示削除 + メタ表示追加**

---

## 8. テスト観点（最低限）

- ホームタブが消えていること
- 4タブで遷移できること
- ②画面：4本のラインが正しい色で描画される
- ②画面：旅行が赤字なら、線が赤くなる + フッターが赤字メッセージ
- ②モーダル：+を押すと数値が増え、限度額を超えると無効化される
- ②モーダル：操作中、背後のメイングラフが連動して動く
- ②モーダル：「期間を選ぶ」を押すと俯瞰グラフが展開、年齢軸が表示
- ②モーダル：範囲スライダーを動かすと、選択範囲の帯と年齢ハイライトが連動
- ③画面：計画/実績の2カラム構造が消えていること
- ③画面：「+支出を追加」→ 教育費テンプレート → 公立/私立選択 → 自動入力
- ③画面：単位切替（年/月）で数値が正しく換算される

---

## 9. 設計上の補足・残された議論ポイント

実装には影響しないが、レビュー時に確認が必要な事項：

- **シナリオタブ**の機能は今回の改修には含まれない。ただしタブとしては残る
- **複数プラン**（プランA / プランB）の切り替えは別途検討
- 現状、家族メンバーの登録UIは存在しない前提。モックデータで `あなた=36歳, 妻=34歳, 子=1歳（2025年時点）` などをハードコードして良い
- **オンボーディング**：新規ユーザーが①→②→③を自然に進める誘導フローは別途検討
- **整合性チェック通知**：差分があった時のプロアクティブ通知は本リリースには含まない
- 「ツカイドキ＋」（有料版）として、計画 vs 実績、毎月の振り返り、整合性自動通知などを切り出す構想あり
- **支出テンプレートの参考値**：本実装ではハードコードでOK。将来的にはAPI/DBから取得する想定

---

以上が実装指示書です。不明点があれば実装着手前に質問してください。
