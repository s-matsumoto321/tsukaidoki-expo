# ツカイドキ デザインリニューアル実装指示書

## 概要

既存の React Native アプリ「ツカイドキ」のビジュアルデザインを刷新する。
旧時代的な濃紺ヘッダー＋ベタ塗りのデザインを、モダンな「セージ＆クリーム」配色へ移行する。

**重要：機能・画面構成・ナビゲーション構造は変更しない。** 純粋にビジュアル層（色、タイポグラフィ、コンポーネントのスタイル、レイアウトの余白）のみを刷新する。

---

## 0. トーン＆マナー（最重要・全実装の判断基準）

実装中に「この見せ方で合っているか？」と迷ったときは、必ずこのセクションに立ち返ること。
カラートークンや個別の指示よりも、このトーン＆マナーが上位の判断基準となる。

### 0.1 アプリのビジョン（前提）

ツカイドキは「**暗中模索の人生に、霧を晴らす地図を提供するアプリ**」である。
ユーザーに以下4つの感情を与えることを目指す：

- **安心** — 老後・教育・もしもの事態でも生きていけるかへの答え
- **希望** — 自分にも車や海外旅行という未来があるという感覚
- **冒険心** — 保守的な人生から、もう一歩踏み出してみる勇気
- **自信** — 苦手な財務管理ができている実感

カーナビのように「**推奨ルートを提案するだけ**」で、目的地もルート選択もユーザーが決める。
過去の振り返り機能は持たず、**常に「現在と未来の関係」だけ**を映す。

### 0.2 デザインのキーフレーズ

> **「朝の光の中で開く、自分の人生の地図」**

このフレーズに合わない要素は、たとえ機能的に正しくても採用しない。

### 0.3 7つのトーン原則

1. **静謐（せいひつ）** — 騒がしくない。叫ばない。鎮静の色を選ぶ。彩度を抑える
2. **温度がある** — クリームとセージで体温を持たせる。冷たいデータ画面にしない
3. **物語的** — 数字を「データ」ではなく「物語の一節」として扱う（Fraunces セリフ書体）
4. **手帳的** — 紙の質感、親密さ。業務システムではなく、ユーザー自身の持ち物の感覚
5. **余白を恐れない** — 情報密度より呼吸。霧の中でこそ、隙間が大事
6. **そっと差し出す** — フローティング、控えめなシャドウ、彩度を落とした色。押し付けない
7. **祝祭は控えめに** — ハニーゴールドの小さな光。派手な達成演出はしない

### 0.4 NGリスト（このアプリで絶対にやらないこと）

- ❌ 「目標達成！🎉」のような派手な祝福、紙吹雪アニメーション、勝利ファンファーレ
- ❌ 赤い警告色、黄色いビックリマーク連打、`#FF0000` 系の純粋赤
- ❌ ネオンカラー、ビビッドなグラデーション、ガラスモーフィズム過多
- ❌ 「節約しましょう」「無駄遣いです」のような断定的・指導的な文言
- ❌ ゲーミフィケーション過剰（バッジ収集、経験値、ランキング、連続記録）
- ❌ スタートアップ的な強い色使い（鮮やかな青、ビビッドな緑、紫グラデーション）
- ❌ 過去ログを強調する UI（「先月のあなた」「3ヶ月前の支出」など）
- ❌ プッシュ通知で焦らせる（「今月は予算オーバーです！」など）

### 0.5 OKリスト（このアプリで大事にすること）

- ✅ 「視界が開けた」感覚を画面で再現する（余白、明るさ、軽さ）
- ✅ 数字を文字として、文章として扱う（Fraunces による詩性）
- ✅ 提案は囁くように、決断はユーザーに委ねる（「〜してみてはいかがでしょう」）
- ✅ 道草を肯定する暖色アクセント（ハニーゴールド、テラコッタ）
- ✅ 紙の手帳のような親密さと、デジタルの正確さの両立
- ✅ 未来を見せる UI（達成予測、シナリオ比較、夢の可視化）
- ✅ ユーザーが主役、アプリは黒子（カーナビ的な距離感）

### 0.6 配色がビジョンを体現する仕組み

なぜこの配色なのかを、開発者・デザイナーが理解しておくこと：

- **クリーム背景（#FAF7F2）** = 朝の光、霧が晴れた直後の空気感。「視界が開けた」感覚
- **セージグリーン（#5B8E7D）** = 鎮静の色、身体的な安心感。青の「冷静」とは違う、自然治癒的な落ち着き
- **ハニーゴールド（#E8B86D）** = 収穫、夕暮れの光、小さな祝祭。「道草を肯定」する暖色
- **彩度を落とした全体** = 「押し付けがましくない」を色で実現。鮮やかな色は主張が強すぎる
- **Fraunces（セリフ書体）** = 数字を「帳簿」ではなく「人生の物語」にする
- **フローティング UI** = 「アプリ全体が一枚岩のシステム」ではなく「必要な情報がふわっと立ち上がる」カーナビ的体験

### 0.7 マイクロコピーのトーン

UI 上のテキスト全般について：

| シチュエーション | NG（やらない） | OK（こう書く） |
|---|---|---|
| 警告 | 「予算オーバーです！」 | 「残高修正が必要です」 |
| 提案 | 「節約しましょう」 | 「積立額を見直すと改善できます」 |
| 達成 | 「目標達成！おめでとうございます🎉」 | 「順調に積み上がっています」 |
| 未達成 | 「未達成」「失敗」 | 「要注意」「達成が遅れ気味です」 |
| 誘導 | 「今すぐ設定！」 | 「設定しておくと便利です」 |
| 状態説明 | 「4PJ・37夢・5口座 管理中」 | 「4 プロジェクト ・ 37 の夢 ・ 5 口座」 |

文末は断定より提案。命令形は避け、ユーザーの判断を尊重する。

---

## 1. デザインシステム

### 1.1 カラートークン

`src/theme/colors.ts`（または相当する場所）に以下のトークンを定義し、全画面で使い回すこと。ハードコードされた色は禁止。

```ts
export const colors = {
  // 背景
  bg: '#FAF7F2',           // アプリ全体の背景（クリーム）
  card: '#FFFFFF',          // カード背景
  cardAlt: '#FFFEFB',       // カードのバリエーション（ほぼ白）

  // ブランドアクセント（セージグリーン）
  sage: '#5B8E7D',          // メインアクセント、CTA、アクティブ状態
  sageLight: '#8BB0A2',     // セージのサブカラー
  sageBg: '#E8EFEB',        // セージの背景バリエーション（ピル、タブのアクティブ背景など）

  // サブアクセント（ハニーゴールド）
  honey: '#E8B86D',         // 警告、注目、サブCTA
  honeyBg: '#FBF1DD',       // 警告バナーの背景

  // テキスト
  text: '#2C3539',          // 本文・見出し（純黒禁止）
  textMid: '#6B7378',       // 補助テキスト
  textLight: '#A0A6A9',     // ラベル、メタ情報

  // 罫線・区切り
  divider: '#EDE8DF',

  // データ可視化用（円グラフ、棒グラフなど。ブランドカラーとは独立したパレット）
  chart1: '#5B8E7D',        // 主要セグメント
  chart2: '#4A7FA8',        // 副次セグメント（ブルー）
  chart3: '#E8B86D',        // ハニー
  chart4: '#C8826B',        // テラコッタ
  chart5: '#B8AFD9',        // ラベンダー
  chart6: '#BDC4C7',        // ニュートラルグレー（その他用）
};
```

**色の使用ルール：**
- 紺色（旧 `#0F2A4A` 系）は完全に排除する
- メインアクセントは `sage` を使い、CTAボタン・選択中タブ・進捗バー・フォーカス要素に適用
- 警告・注意系は `honey` 系を使う（赤は使わない、温かみを保つ）
- 既存の用途別カラー（教育資金=青、車資金=オレンジなど）は `chart1〜6` のパレットへ置き換える

### 1.2 タイポグラフィ

```ts
export const typography = {
  // 数字・見出し用：Fraunces（Google Fonts）
  display: 'Fraunces',         // 金額、ページタイトル
  // 本文用：Noto Sans JP
  body: 'NotoSansJP',
};

export const fontSizes = {
  pageTitle: 32,    // Large Title
  amountHero: 40,   // 総資産などの主要金額
  amountMedium: 18, // 円グラフ中央値、カード内金額
  heading: 16,
  body: 14,
  caption: 12,
  micro: 10,
};
```

**ルール：**
- 金額・ページタイトルには Fraunces（セリフ系）を使用し「ジュエリー感」を出す
- それ以外は Noto Sans JP
- フォントは `expo-font` または `react-native-font` で読み込む。読み込み完了までは Splash で待つこと

### 1.3 スペーシング・角丸

```ts
export const spacing = { xs: 4, sm: 8, md: 12, lg: 16, xl: 20, xxl: 24, xxxl: 32 };
export const radius = {
  sm: 12,    // 小さなバッジ、ピル内の要素
  md: 16,    // アラート、小カード
  lg: 24,    // 標準カード
  xl: 28,    // ヒーローカード
  pill: 100, // ピル、タブのアクティブ背景
  full: 9999,
};
```

### 1.4 シャドウ

iOS では薄め、Android では `elevation` で代替。CSS でいうと以下のレベル感：

```ts
export const shadows = {
  card: {
    shadowColor: '#2C3539',
    shadowOffset: { width: 0, height: 1 },
    shadowOpacity: 0.04,
    shadowRadius: 3,
    elevation: 1,
  },
  floating: {
    shadowColor: '#2C3539',
    shadowOffset: { width: 0, height: 8 },
    shadowOpacity: 0.08,
    shadowRadius: 32,
    elevation: 8,
  },
  fab: {
    shadowColor: '#2C3539',
    shadowOffset: { width: 0, height: 8 },
    shadowOpacity: 0.25,
    shadowRadius: 24,
    elevation: 12,
  },
};
```

---

## 2. 共通コンポーネントの変更

### 2.1 ヘッダー（最重要）

**現状：** 全画面の上部に濃紺のベタ塗りバーがあり、「ツカイドキ ライフマネープラン」が表示されている。

**変更：** この濃紺バーを完全に削除する。代わりに：

- 背景はアプリ全体と同じ `bg`（クリーム）にし、ヘッダーバーという概念をなくす
- 各画面の上部にはステータスバーから余白を取った後、Large Title 形式でその画面のタイトルを表示する
- ホーム画面のみ、タイトルを2行構成にする：
  - 1行目：「こんにちは」（13px、`textMid`）
  - 2行目：「あなたの\nライフプラン」（32px、Fraunces、`text`）
- 右上にはハンバーガーメニューを丸型ボタン（40x40、白背景、薄いシャドウ）として配置

**他画面のページタイトル例：**
- シナリオ画面 → 「シナリオ」
- プール金画面 → 「プール金」
- 使いみち画面 → 「使いみち」
- 設定画面 → 「設定」

これらも全て Large Title（32px、Fraunces）で、ヘッダーバーなし。

### 2.2 タブバー（フローティング化）

**現状：** 画面下部に白背景でタブが横並び、シャドウなし、画面に貼り付いている。

**変更：**
- 画面下端から `18px` 浮かせる（`position: absolute; bottom: 18`）
- 左右に `16px` のマージンを取り、画面幅いっぱいではなく「島」状にする
- 背景は `rgba(255, 255, 255, 0.85)` ＋ `BlurView`（`expo-blur` の `intensity={50}`、`tint="light"`）でガラス質に
- 角丸 28px、`shadows.floating` を適用
- 各タブは縦並び（アイコン＋ラベル）、padding `6px 10px`、角丸 18px
- アクティブなタブは背景 `sageBg`、文字色とアイコン色 `sage`
- 非アクティブは色 `textLight`
- アイコンは `lucide-react-native` から：
  - シナリオ → `Activity` または `TrendingUp`
  - プール金 → `Building2` または `Landmark`
  - ホーム → `Home`
  - 使いみち → `Star`
  - 設定 → `Settings`
- アイコンサイズ 20、`strokeWidth: 1.8`

**注意：** タブバーがコンテンツに被るので、各画面の `ScrollView` には `contentContainerStyle={{ paddingBottom: 100 }}` を必ず指定する。

### 2.3 FAB（フローティングアクションボタン）

**現状：** 緑の丸い + ボタンが下部にある。

**変更：**
- 位置：`bottom: 90`（タブバーの上）、`right: 20`
- サイズ：52x52、円形
- 背景色：`text`（`#2C3539`、ダーク系）
- アイコン：白の `+`、サイズ 24、`fontWeight: 300`
- `shadows.fab` を適用

### 2.4 カード

全てのカードコンポーネントの統一スタイル：
- 背景：`card`
- 角丸：`radius.lg`（24）または `radius.xl`（28、ヒーロー用）
- パディング：内容に応じて `lg〜xxl`
- シャドウ：`shadows.card`
- 罫線は使わない（区切りはシャドウと余白で）

---

## 3. 画面別の実装指示

### 3.1 ホーム画面

レイアウト構成（上から）：

1. **ステータスバー余白**（SafeAreaView 任せ）

2. **ヘッダーブロック**（横並び、`paddingHorizontal: 20`）
   - 左：「こんにちは」＋「あなたの\nライフプラン」（2行のページタイトル）
   - 右：丸型ハンバーガーメニュー
   - 下マージン `xxl`

3. **プラン選択ピル**
   - インラインの小さなピル（`alignSelf: 'flex-start'`）
   - 背景 `sageBg`、文字色 `sage`、padding `8px 14px`、角丸 100
   - 中身：左に小さなドット（`sage`）、テキスト「プランA・メイン」、右に下向き矢印
   - タップで現状の「プラン切り替えモーダル/シナリオ画面」を開く

4. **総資産ヒーローカード**（`radius.xl` = 28）
   - ラベル「総資産」（12px、`textMid`、大文字スペース広め）
   - 金額：`¥8,750,000` を Fraunces 40px。`¥` だけ 28px の `textMid` 色で控えめに
   - 区切り線（`divider`、1px）
   - メタ行：「4 プロジェクト ・ 37 の夢 ・ 5 口座」を3カラムで人間的に表示
     - 数字部分は太字 14px、ラベルは 11px `textLight`
     - **重要：** 「4PJ・37夢」のような略語をやめる
   - 装飾：右上に `radial-gradient` で `sage` をうっすら（`overflow: hidden` のカード内に絶対配置の View で表現。React Native では `expo-linear-gradient` の `RadialGradient` または背景透過の `Svg` で代替可）

5. **アラート**（必要時のみ表示）
   - 背景 `honeyBg`、角丸 16、padding 14
   - 左に丸い `!` バッジ（`honey` 背景、白文字）
   - テキスト：「**残高修正が必要です**　差額 ¥100,000」

6. **円グラフ2並び**（横グリッド、gap 10）
   - 各カード：角丸 24、padding 18/16
   - ヘッダー：左に「プール金」または「使いみち」（14px、太字）、右に「口座別」「用途別」（10px、`textLight`）
   - ドーナツ：110x110、ストローク幅 14、`Svg` で実装（現状の実装を流用可）
     - 中央に Fraunces 18px で `¥875万` などの集計値、下に小さく「5口座」
   - 凡例：上位3項目のみ表示（残りは「他」に集約するか省略）。各行は色ドット 7px、項目名、金額（万単位、3桁省略）

7. **AIインサイト**
   - 背景：`linear-gradient(135deg, sageBg, #F0F5F2)`（`expo-linear-gradient`）
   - 角丸 24、padding 20
   - ヘッダー：✦アイコン（`sage`、14px）＋「AIインサイト」（12px、太字、`sage`）
   - 本文：13px、`text`、行間 1.6
   - 装飾：右下に `sage` のラジアル装飾（オプション）

### 3.2 シナリオ画面

- ページタイトルを Large Title「シナリオ」に
- 「現在のシナリオ」見出しは小さめ（12px、`textMid`、大文字）
- 各プランカードは新カードスタイル（角丸 24、`shadows.card`、padding 20）
- 選択中カードは `sage` の 1.5px 罫線＋上部に4pxの `sage` バー（または背景を `sageBg` に）
- 「選択中」バッジは `sageBg`＋`sage` テキストのピル
- 総資産・夢の数は2カラムで、ラベル小・数字大（金額は Fraunces）
- 「もしもプランを追加する」ボタンは枠線なしのカード、左に丸い + アイコン

### 3.3 プール金画面

- ページタイトル「プール金」
- 右上に総残高を Large Title の右にインラインで表示するか、その下に小さくサブヘッダーで配置
- 折れ線/エリアグラフ：色は `sage` 系のグラデーション（`chart1` ベース、透明度で奥行き）
- グラフのX軸ラベルが重なるバグを修正（既存画像で `2086 7` のような表示崩れあり）。`Victory Native` か `react-native-svg-charts` のどちらを使っているにせよ、ラベルの間引き設定を入れること
- 口座リストの各行は新カードスタイル、左に4pxの色付きインジケーター（`chart1〜5` の対応色）
- 「タップで残高を修正」のラベルは `sage`、`textDecorationLine: 'underline'` でリンク感
- 下部の「積立・複利を調整する」ボタンは：背景 `sage`、白文字、角丸 16、padding 16、影 `shadows.card`

### 3.4 使いみち画面

- ページタイトル「使いみち」
- 上部の積み上げ棒グラフ＋凡例カードはそのまま、色だけ `chart1〜6` に置換
- AIインサイトはホームと同じスタイルを使い回す
- 「カスタム / 緊急順 / 時期順」のセグメント：
  - 選択中：背景 `sage`、白文字
  - 非選択：背景透明、文字 `textMid`、薄い罫線（`divider`）
- 各夢カード：
  - 左に4pxの色バー（用途別カラー）
  - タイトル＋進捗バー＋金額＋達成率のレイアウトはそのまま
  - 「要注意」バッジ：背景 `honeyBg`、文字 `honey`、ピル形状
  - 「順調」バッジ：背景 `sageBg`、文字 `sage`、ピル形状
- 進捗バーの色は用途別カラーを使い、未達成部分は `divider`

### 3.5 設定画面

- ページタイトル「設定」
- セクションヘッダー（「資産管理」「プロフィール」など）：12px、`textMid`、大文字スペース広め
- 各項目カード：左に4pxの色バー（セクション別に色分け）、タイトル＋サブテキスト、右に `>` 矢印
- 色バーの色割り当て：
  - 資産管理 → `sage`
  - プロフィール → `chart1`（セージ）または `chart2`（ブルー）
  - 連携 → `honey`
  - プラン → `chart5`（ラベンダー）
- 「プレミアム会員」項目には小さい王冠アイコン（lucide の `Crown`）を追加してもよい

---

## 4. アニメーション・インタラクション

最低限以下を実装：

- **タブ切り替え：** アクティブタブの背景色（`sageBg`）が 200ms でフェードイン
- **カードタップ：** `Pressable` の `onPressIn` で `scale: 0.98`、200ms スプリングで戻す（`react-native-reanimated` の `useAnimatedStyle`）
- **画面遷移：** デフォルトの `react-navigation` のトランジションでOK、ただしヘッダーがないので `headerShown: false` を全画面に適用
- **金額の表示：** 初回ロード時に金額をカウントアップさせる（`react-native-animatable` または手動で `useEffect` 内で setInterval）と高級感が出る。優先度は低め

---

## 5. 実装順序の推奨

1. `colors.ts`、`typography.ts`、`spacing.ts` のトークンを定義
2. フォント（Fraunces、Noto Sans JP）を `expo-font` で読み込み
3. 共通コンポーネント `<Card>`、`<PageTitle>`、`<Pill>`、`<TabBar>`、`<FAB>` を作成
4. 全画面の濃紺ヘッダーを削除
5. ホーム画面を新デザインで再構築
6. シナリオ → プール金 → 使いみち → 設定 の順に各画面を移行
7. アニメーションの追加
8. ダークモード対応（後回し可、ただしカラートークンの構造を最初から `light` / `dark` で分岐できるようにしておくこと）

---

## 6. 動作確認チェックリスト

- [ ] 濃紺ヘッダーが全画面から消えている
- [ ] 全画面でページタイトルが Fraunces で表示されている
- [ ] タブバーがフローティング化され、画面下端から浮いている
- [ ] タブ切り替え時にアクティブ状態の背景色が変わる
- [ ] FAB が画面右下、タブバーの上に正しく配置されている
- [ ] スクロール時にタブバー裏のコンテンツがぼかして見える（BlurView 動作確認）
- [ ] 全ての金額表示が Fraunces で `¥` が小さく表示されている
- [ ] 円グラフの中央値が正しい
- [ ] アラート（残高差異）が `honey` 系の色で表示されている
- [ ] AIインサイトが `sageBg` のグラデーション背景で表示されている
- [ ] iPhone SE（小画面）と iPhone Pro Max（大画面）の両方で崩れない
- [ ] ダークモードに切り替えてもクラッシュしない（対応は後回しでも、最低限落ちないこと）

---

## 7. 参考モック

リファレンス用のホーム画面 HTML モックを別途共有する(tsukaidoki_home.html)。色・余白・配置はモックを基準にすること。コード自体は HTML/CSS なので React Native への移植は必要だが、ビジュアルゴールはこれ。

---

## 8. やらないこと（重要）

- 機能追加・削除
- ナビゲーション構造の変更
- データモデル・API 通信ロジックの変更
- 画面の追加・削除
- 入力バリデーションロジックの変更

純粋にビジュアル層のみのリファクタリングであることを徹底すること。
